# 照片目标物件关联：成对小样本验证

运行前固定。基线为 v18 精确部署代码 SHA `a5316407ea6ce60c7d2decef482c31c469bda7b6fa88565154348e9ccd22433a`。不改模型、采样、图像、其他照片规则、事实审核或趣味阈值。

四张已人工查看的授权无人物公开图，每张含一个正例和一个反例：清晰部件可关联；不存在的对象、不可见材质/传感器、错误外观必须拒绝。人工标签与内容固定在配套 JSON。这里的文字是人工编写的关联测试夹具，不代表 AI 生成质量、可靠事实或用户卡片。

唯一改动：目标对象提示明确检查画面中可辨认的对象/部件，不要求它是占画面最大面积的主体；仍要求直接可见依据，不能从候选推断未拍到的物件。先保留为实验，不接入默认主干或部署。

8 条件×基线/候选各一次，最多 16 次真实视觉核验；模型为既有配置的 Qwen VL Plus，0 搜索、0 额外图片服务。两种提示按条件交替先后，每次串行，失败不重试，保留所有超时与错误；鉴权或限流失败后停止本轮。不是生产接口端到端，也不是统计显著性试验。

候选进入后续验证的最低门槛：8/8 符合人工标签且结构完整，无反例误放；比基线多通过至少一个正例。未满足则不采用；满足也仅允许扩大独立图片与完整生成验证，不能称为稳定出卡或上线验收。

## 第二次实验（首次结果后固定）

v1 基线 6/8、候选 7/8；两者都误放“照片中手柄内置霍尔传感器”。保留 v1 及全部回执，不采用。v2 不改变标签或模型；移除默认照片规则中未经验证的“这条卡片适用于整个基础类别”断言，明确对象可见不代表个体内部断言成立。先跑原 8 条候选条件一次，0 自动重试；全过才使用另一组 4 张新图片/8 条已固定反向条件验证。原基线不重跑，不伪称新的成对试验。每组最多 8 次视觉调用、0 搜索。

v2 仍须原组和未见图组均 8/8 且无不完整输出，随后才考虑完整生成链路。没有减少原本的来源、范围、隐私或趣味要求。

## 固定样例上的模型对比（v2 未见图结果后固定）

v2 原组 8/8、未见图组 7/8，漏认车轮中央的固定螺母，仍不满足采用门槛；螺钉正例的观察文本还误写了槽型，不将分类标签正确等同于每条观察都准确。下一步不继续针对失败物件添加提示，而保持 v2 提示与全部 16 个条件不变，改用已配置的 `qwen3.7-plus-2026-05-26` 做一次对比。官方视觉文档明确该快照支持图像输入与结构化输出。

本次只改变模型，16 次调用、0 搜索、0 重试。它使用已经暴露结果的对比集，不再称为未见图盲测。16/16 且无不完整结果才允许进入完整生成验证，观察描述仍需人工查错；若模型表现也不足，保留实验，不换线上模型。照片匹配、事实支持、趣味与个人状态不可推断仍是独立条件。

## 完整生成小试（模型对比结果后固定）

Plus 的 16 项二分类全部匹配，允许继续小试；但描述中把分针位置当时针等细节仍有误，不把“分类16/16”写成“视觉细节100%准确”。固定三张公开脱敏图 web-059、web-011、web-022，各从本地空库调用一次完整链路，总上限 36 模型请求/3 搜索，无自动重试。任何依赖鉴权或限流错误后不启动下一图。

基线代码仍是 v18，只在视觉复核的出站请求应用 v2 上下文和 Plus 模型；识别、搜索、写作、来源与趣味规则不动。生成顺序和结果完整记录。至少 2/3 首次真实新卡、所有出卡均经人工核对来源与照片，才允许考虑采用；不达标则不换线上模型。没有同期重新随机生成的对照组，不能把本次最终产出与旧结果直接当提示因果提升或统计结论。

结果：三张全部 `no_insight`，0/3，没有达到采用门槛。前两张照片核验通过但证据不通过；第三张因来源组合/趣味不足未进入照片核验。本轮不采用实验，不更换线上模型。人工复核还发现部分被证据审核通过的分句扩大了来源范围，不能只删掉审核否决的一句话。完整结果见 `docs/PHOTO_ASSOCIATION_CALIBRATION_2026-09-09.md`。
