# 见微生产 Beta 交接

## 当前目标与范围

把中国大陆 Android 照片冷知识组件推进到可由真实证据放行的受控 Beta。当前发布结论是 `NO_GO`；
不得用模拟器、合成证据、测试签名、Kimi 审查轮次或本地自测代替真人内容审核、真实云、实体机和真实 cohort。

## 已完成并验证

- 2026-07-26 已收口系统组件预览增量暴露出的两条 App 设备回归不稳定，未改生产业务逻辑。`DailyHistoryNavigationInstrumentedTest` 的日期夹具改为与产品一致的 `Asia/Shanghai` `ChinaCalendar.today()`；当 LazyColumn 尚未组合目标节点时改用小步手势，不再用整页滚动跳过中间正文，已有节点则优先 `ACTION_SHOW_ON_SCREEN`。`OnboardingValuePreviewInstrumentedTest` 不再把“偏好已写入”误当成系统 Photo Picker 已出现：现在等待外部窗口后才返回，并等待 App 窗口恢复；点击每次从当前语义树重新获取具有可点击祖先的节点，避免第 3 页标题与按钮同名“仅选择照片”时误抓不可点击标题。两类测试统一用当前无障碍根窗口的整屏坐标判断可见性，修复 1.6× 字体下肉眼可见、`visible=true` 的底部按钮被 App 资源窗口高度误判为屏外；失败会输出当前语义树、目标边界和可点击祖先边界。最终 API 34 标准字号下首次体验连续 5 轮共 10/10、每日历史连续 5/5 通过，1.6× 字体专项 3/3 通过并恢复 1.0；最终完整 App instrumentation 为 19/19（66.217s）。此前诊断阶段一轮在多轮 ADB 重跑后异常停顿且未输出失败，物品提醒专项随即 1/1 通过，之后三次完整套件均为 19/19，未再复现。App JVM、Debug Lint、Debug APK、androidTest APK、源码门禁和差异检查均通过。原先“全量 App instrumentation 未全绿”的 caveat 已关闭；外部 Beta 证据与 `NO_GO` 结论不变。
- 2026-07-26 系统“添加到主屏幕”决策页已从只有“正在准备今天的卡片”的加载占位，升级为真实 2×2 价值预览：复用仓库自有无人物扫帚图，展示“见微 · 示例”、对象问题和知识目录中“扇形刷毛更容易贴近墙角”事实的保守改写；“示例”与图片无障碍描述均显式存在，不冒充用户照片。Provider 的运行时 `initialLayout` 继续使用诚实加载态，仅 `previewLayout` 指向静态预览，未改变 Glance、Room、扫描或上传边界。API 34 Pixel Launcher 改前、标准字号和 1.6× 字体的真实系统 Pin 页均已截图看图；大字号下标题/事实完整、照片可辨识，系统字体已恢复 1.0。新增 `WidgetPreviewMetadataInstrumentedTest` 证明 Provider 元数据、示例标识、图片资源和运行时加载布局边界，专项 1/1 通过；小组件安装、刷新设备测试也分别通过，App JVM、Debug Lint、Debug assemble、源码门禁和差异检查通过。Debug APK SHA-256 为 `453dabdac4a788dcae0a7442367ae7b2e393f3435cbf2d86ec0cb83508435c25`；改前/标准/1.6× 截图 SHA-256 为 `20071d46f2235c604905a40cd4c88982ef2cc280ffe6a03a518de17f7a269631` / `b2bb1f8d47f243a88a5af4970b739cb27c99a6ce18cf2f750ca8c6e72e9f9e85` / `4b46a831637c59614309a3557d68fdef1eef228e7065084d0ad0a691972db4d9`，位于忽略目录 `.tooling/widget-preview-audit-20260726/`。以上均为本地模拟器工程证据，Beta 仍为 `NO_GO`。
- 2026-07-26 已在当前 API 34 APK 上重走“应用入口 → Pixel Launcher 系统 Pin → 2×2 → 4×2 拖拽”，用仓库自有扫帚图、当日卡、来源和次日缓存验证宽组件完整显示原图、知识、来源和“换一条”，旧审计图的大块留白不是当前回归。真正缺口是系统首次只展示 2×2，而应用未说明 4×2 的存在；现在首卡后的 CTA 明确写为“添加后可长按调整为 4×2，查看来源并换一条”。标准 1080×2400 保持横排，1.6× 字体自动纵排且按钮完整可达。新增 `WidgetResizeDiscoveryCopyTest`；目标单测、完整 App JVM、Debug assemble、源码门禁和差异检查均通过。Debug APK SHA-256 为 `a832b129e7af03ad582bf3c40f1044cbdea92fd114303b5029f427a22494c156`；截图位于忽略目录 `.tooling/widget-design-audit-20260726/`，均为本地工程证据、`releaseEvidence=false`，Beta 仍为 `NO_GO`。
- 2026-07-26 已用用户下载的北京百炼工作空间按量付费 Key 和仓库自有无人物扫帚图执行真实 Qwen Provider 验证。固定模型 `qwen3.6-flash-2026-04-16` 在无内容安全诊断路径识别为“扫帚”、置信度 0.98，证明 Key、工作空间端点和模型可达；生产强制 `X-DashScope-DataInspection` 的请求返回 `403 access_denied`，精确阻断为同一工作空间尚未授权 AI Safety Guardrails。服务器运行时不允许省略该 Header。验证器已收敛为一份 `0600`、`wx`、`releaseEvidence=false` 的机读报告，记录本轮实际 3 个请求、净化图片摘要和明确下一动作；写出前拒绝 API Key、完整端点和本地输入路径泄漏。真实脱敏报告位于忽略目录 `.tooling/qwen-provider-verification-2026-07-26.json`，SHA-256 `4cbb927227fad5dcaf3f362f3081f6db33e2cb421de03ab9a6ae528a29a1ff2b`。下一外部动作是由阿里云主账号开通按量付费 AI Safety Guardrails、授权当前百炼工作空间并允许创建 `AliyunServiceRoleForSFMAccessingCIP`，然后重跑验证；该本地诊断不改变 Beta `NO_GO`。
- 2026-07-25 “每天一张”已从单次任务限额修正为真实的中国自然日限额。此前 `DAILY_ONE` 虽然单个 Worker 最多上传 1 张，但首次扫描、权限调整、模式切换或 WorkManager 重试仍可能在同一天启动多个自动任务；现在 Data 层用同步、立即落盘的本地配额保存 `day + candidateLocalId`，同一天不同候选失败关闭，同一候选允许在进程/任务中断后幂等继续，次日自动重置。PrivacyScanWorker 在当天已有配额时不再检查新的自动批次；UploadWorker 重启后直接按已领取 ID 从 Room 精确取回 READY 候选，不会被更高分照片挤掉。配额只作用于 `MEDIA_STORE + DAILY_ONE`，提前准备和用户显式 Picker/分享不受影响。设置、引导后状态和反馈文案统一为“每天/每个自然日最多上传分析 1 张”，不再使用“每个周期”。API 34 真实设备测试覆盖同日并发 24 个候选仅 1 个新领取、仓储重建、同候选重试、跨日重置、损坏状态失败关闭、Room 精确取回和显式导入隔离；最终 Data instrumentation 64/64、App instrumentation 18/18，Android JVM 184/184（Domain 60、App 46、Data 78），App/Data 双 Lint 0 error，Debug、R8 Release、源码门禁和差异检查通过。2× 字体测试还在保留一条既有导入交接状态时验证专项会暂时清空并原样恢复该状态，按“标题 → 自然日说明 → 开启按钮”连续阅读通过。标准/大字截图 SHA-256 为 `3e3dfec9649d333ad0621529753f38be9b7e1b4da89079422afb56806d7820d8` / `fb39de4319bf2b291f1d227099524a46032dd5b51dedb05be1b2abfcc4629ca5`，位于忽略目录 `.tooling/daily-one-quota-audit/`；Debug/未签名 Release APK SHA-256 为 `8112a59a834d39d4f5faa0f748fc07177c3548e631fb82d05987c2502b54caf2` / `54dd161a44f553abdb2ca459b3f1788e69e1bdd8db9750bd4ceb239e08261641`。本轮不改变真人知识审核、真实云、正式签名和 cohort 等外部证据，Beta 仍为 `NO_GO`。
- 2026-07-25 双模式已从首次选择继续贯穿到真实使用状态。此前用户选择“每天一张”后，首页空状态和隐私中心仍可能显示“持续补充未来卡片”等卡池文案；现在 `DiscoveryUiPolicy` 以持久化的 `AutomaticCardMode` 同时决定自动发现说明、相册访问摘要、启动反馈和 QUEUED/SCANNING/FILTERING/SYNCING/NO_MATCH 状态。“提前准备”明确逐步准备 7–14 张卡；“每天一张”明确每个自动周期最多上传分析 1 张、本机最多深度检查 4 张、未可靠命中时不为凑数生成卡片。Picker-only、部分授权和完整授权仍分别说明实际范围。API 34 标准字号测试真实点击“开启自动发现”进入系统权限页；840×1867/2× 字体验证同一说明与可交互按钮完整可达；当前完整 App instrumentation 18/18，Android JVM 183/183（Domain 59、App 46、Data 78），App/Data 双 Lint 0 error，Debug、R8 Release、源码门禁和差异检查通过。标准/大字证据截图 SHA-256 为 `0bc51946e9c7f4932e057765045c7af8a12d2eac97d777a48024d54e9eb491f4` / `afcc9c86dac584c8b4bb82059bb6bc7dae417652c9aa0497b53791f252c9a38b`，位于忽略目录 `.tooling/mode-aware-status-audit/`；当前环境未完成人工看图，只把设备语义断言、尺寸与哈希作为已验证证据。Debug/未签名 Release APK SHA-256 为 `e314bc83bcbcaa45787230f5400a6cd2543704cab6db2d1d94bb964eb87bf374` / `8121a876b7ede39b285728dda8ed916cd73fc38fdbea1eec24e2cdd71d5f2b9a`。本轮不改变外部 Beta 证据状态，发布仍为 `NO_GO`。
- 2026-07-25 双照片处理模式已进入首次体验，不再只藏在完成引导后的设置页。三屏结构保持不变：第三屏同时完成恰好 3 个兴趣、自动发现“提前准备（推荐）/每天一张”节奏和“自动发现/仅选择照片”授权方式的选择；显式说明节奏只约束自动发现，Photo Picker/分享仍按逐次同意处理。两条入口会在离开引导前共同持久化兴趣和节奏，因此 picker-only 用户以后开启自动发现也沿用原选择。引导页码、兴趣和模式改为 `rememberSaveable`，API 34 设备测试真实修改兴趣、选择“每天一张”、重建 Activity，再从“仅选择照片”进入系统 Picker，并验证 `DAILY_ONE` 已持久化；标准 1080×2400 与 840×1867/2× 字体均通过。完整 App instrumentation 18/18；JVM 182/182（Domain 59、App 45、Data 78），App/Data 双 Lint 0 error，Debug、R8 Release、源码门禁和差异检查通过。标准/大字第三屏截图 SHA-256 为 `db7315d80cb994defcdb5b3cac294c96f65e798ee621044ca32a899d88c73f8d` / `4b15aeb846f12ceffbe10a6c0bc9807c78bdbd2cace382fc6c063c7e0a10421b`，位于忽略目录 `.tooling/onboarding-mode-audit/`；Debug/未签名 Release APK SHA-256 为 `542e08dd73730235bd441ed51315c65939920cc10b79ee79d2326b947a894565` / `b9b634f56f49cdee5798dd4557167e0afad805e2e5dcf086fd45332b46e6ffd6`。本轮遵守 Android Clean Architecture：Compose 只维护未提交的引导选择，ViewModel 编排，实际持久化继续经 domain Repository 边界进入 data；外部 Beta 证据不变，发布仍为 `NO_GO`。
- 2026-07-25 已按产品决策落地两种可持久化的自动照片处理节奏。默认“提前准备（推荐）”沿用低于 7 张时补到最多 14 张未来/当日卡、单轮最多上传 24 个自动候选的离线可靠卡池；“每天一张”每次自动周期最多上传并分析 1 个候选，本机隐私筛选最多检查 4 张来寻找 1 张安全且不重复的照片，失败时继续显示已有卡，不冒充已生成新知识。设置页以单选语义展示两种模式及隐私/失败边界，选择会跨 Activity 重建保留；重复点击当前模式不重复排程，暂停分析或仅选择照片时只保存设置。切换模式不会删除既有卡，Photo Picker/分享导入仍按用户逐项同意的独立链路处理。完整 App instrumentation 17/17、Data instrumentation 60/60、Android JVM 182/182，App/Data 双 Lint 0 error，Debug、R8 Release、源码门禁与差异检查通过。标准/2× 字体设置截图 SHA-256 为 `8f7076ccca925a7ed160278d845b7a9e4e0522979c1ccd4f4d4a84055dfdef25` / `ea2c58f2008cd9999f04a02b2d7c7bc8c14716287acc70a3e4b3674da17a6eb8`；Debug/未签名 Release APK SHA-256 为 `f89b63b1cf2a096e2e1a5da0e7f87f771bd25dac6c3876b348339d99548f8e70` / `fbb4f144b670cc7ada57f6015685e2d6eb205ba37a326cfd1ac9d380f6711453`。这些仍是模拟器和本地工程证据，外部 Beta 结论保持 `NO_GO`。
- 2026-07-25 首页内容与设置已完成信息架构分离。原先“每日/收藏”入口会随 LazyColumn 内容滚走，推荐偏好和隐私管理又长期挂在每日卡片末尾；当前内容区顶部固定显示“每日 / 收藏 N / 设置”三段入口，每日只负责今日与往日知识，收藏只负责归档，设置独立承载推荐兴趣、照片发现方式、桌面组件和数据管理。三个页面分别持有 LazyListState，离开设置深处再返回会恢复原滚动位置；收藏或设置按系统返回键统一回每日，精准卡片详情继续隐藏全局入口并保持原返回优先级。设备实跑发现 Material Button 会把 `selected=true` 与可访问名称拆成父子节点，现改为 48dp 自绘分段按钮，以单一 TalkBack 节点同时暴露 Tab 角色、名称、选中态和点击动作。改动只在 Compose 展示/导航及对应设备路径，Domain、Room、Repository、扫描、同步和组件业务未改。API 34 真实 Room 测试验证每日不含设置表单、设置不含知识卡、三段选中态、设置底部操作、固定入口、滚动位置恢复和系统返回；标准 1080×2400 与 840×1867/2× 字体均通过。标准/大字设置首屏截图 SHA-256 为 `5f556634b7871ffdd95c454fb575fbf4e41cad84ab7f5440a7c18e32bbdc2ba5` / `9462f1f96cd2e144e0cd344dd540b45119c9662365a72e4af56e5e37bbe86662`，位于忽略目录 `.tooling/settings-navigation-audit/`，只属本地工程证据。完整 App instrumentation 17/17；Android JVM 179/179（Domain 56、App 45、Data 78），App/Data Lint 0 error，Debug、R8 Release、源码门禁和差异检查通过。Debug/未签名 Release APK SHA-256 为 `aa20d672eb8a92c1c46454fece4a748d4e8e2dbcf200c18c2b8d51bc2ff3405d` / `0398355c10c69fd33b5835063071a8cf09a2631e6f50b7993857965eb1786c64`。外部 Beta 证据状态不变，发布仍为 `NO_GO`。
- 2026-07-25 每日页已从“今天与全部历史卡都逐张展开”的长表单收口为真正的一日一知体验：今天的卡片继续完整展示照片、知识、来源、提醒和反馈；历史卡统一进入“往日一知”紧凑目录，只展示日期、标题和摘要。点击任一旧卡会在每日上下文中打开完整详情，显式返回和系统返回都会回到原历史列表位置；历史详情仍使用既有收藏、提醒和反馈能力。改动只位于 Compose 展示与本地导航，DailyCard domain policy、Room、Repository、卡池补给和组件调度均未改变。新增 API 34 设备测试用真实 Room 的今天/昨天/更早三张卡验证今天正文、历史不提前展开私人上下文、旧卡完整来源与返回位置，并在标准 1080×2400 和 840×1867/2× 字体下通过。标准目录/详情截图 SHA-256 为 `b62958b013b57ec04811d6c1e897af0e780bdc983396b1f4f7cc79a5b52f60b3` / `e3d9c5830e446e99d665b18579d5679ee805c1b3abfb3f48`；大字目录/详情为 `3f36e5620443589eee64bfe735f006faabd484e1e41ee98dcbc99a6901bc6813` / `56fe3b3ba238124b34c16d976207a96e7edaa1a0c8f2657d804dafd1064d6880`，均位于忽略目录 `.tooling/daily-history-audit/`，只属本地工程证据。完整 App instrumentation 16/16；Android JVM 180/180（Domain 56、App 46、Data 78），App/Data Lint 0 error，Debug、R8 Release、源码门禁和差异检查通过。Debug/未签名 Release APK SHA-256 为 `76cbd8c5c8be36008d78527ca0350a86c7d5e4fecf9b9d30d0b025aa430ce43e` / `3ff2a8606095b10d890164c4b76f7874a5193c282ee84ced56ad48be52efd240`。外部 Beta 证据状态不变，发布仍为 `NO_GO`。
- 2026-07-25 物品提醒已从“选择日期和周期后直接确认”的工具表单收口为用户主动建立、不会冒充 AI 判断的复查约定。新建与编辑都使用知识卡的规范对象名，首先明确“见微不会从照片猜测这个物品用了多久”；开始使用日和预计复查日改为中文日期，预计提醒说明为当天上午 9:00 左右且系统省电可能延迟。用户必须勾选“我确认以上开始使用日和复查周期”后才能提交，修改日期/周期会撤销确认，再次编辑也必须重新确认；同时明确这是自定义复查提醒，不代表专业更换建议。已开启状态不再展示原始 ISO 字符串，而是分别显示开始使用日、复查周期和预计提醒日。长内容可滚动，操作区保持可达；Reminder/WorkManager/Room 业务边界未改，Android Clean Architecture 仅用于约束本轮变化留在 Compose 确认与展示层。新增 API 34 设备测试使用真实 Room 卡片验证默认禁用、确认后启用、提醒持久化为当天/90 天、中文状态可见以及编辑重新确认；标准 1080×2400 与 840×1867/2× 字体完整链路均通过。标准/大字截图 SHA-256 为 `b7282fdb5a6cd2ce37118d5e060693e179b344021c257a97ab115ffe3c11694b` / `5cb486724be3ba4941f132fed1c1985bcdde5ad29cfee510cf17b9fd8b7af4e7`，位于忽略目录 `.tooling/reminder-experience-audit/`，只属本地工程证据。完整 App instrumentation 15/15；Android JVM 180/180（Domain 56、App 46、Data 78），App/Data Lint 0 error，Debug、R8 Release、源码门禁和差异检查通过。Debug/未签名 Release APK SHA-256 为 `a8651f2bf5ed76403bf3df29501e1ff49497677c0b2e7e663ba6e27091026e0b` / `ad332d838b505a8ad6f796ef86a9b09ff52d08bafb60f1d4996060a0c2c3b5f5`。外部 Beta 证据状态不变，发布仍为 `NO_GO`。
- 2026-07-25 有内容的收藏页已从逐张展开完整知识卡改为可长期使用的归档目录。收藏卡按既有 Room `savedAtMillis DESC` 顺序展示紧凑照片、日期、标题、知识摘要和“查看完整知识”；标准屏一屏可浏览两张，容器净宽 `<300dp` 或字体 `>=1.5×` 时切为照片在上、文字在下，避免 2× 字体被窄列逐字挤压。点击目录卡进入完整来源/提醒/反馈详情，显式“返回收藏”和系统返回键都恢复原收藏滚动位置；在详情取消收藏后，Room 状态变为未收藏、详情自动关闭、计数实时减少，其他收藏不受影响。详情仍用 1280px 有界缩略图，目录预览改用 320px 解码；无图占位统一为小字号居中两行。详情滚动复位只绑定当前卡片 ID/存在性，后台其他收藏更新不会把阅读位置拉回顶部。改动只位于 Compose 展示/导航和既有解码参数，Room/Repository 收藏语义未改。API 34 设备测试以真实 Room 两张收藏验证紧凑目录不提前展开“为什么推给你”、进入详情、系统返回、再次进入、取消收藏和最终 DAO 状态；标准 1080×2400 与 840×1867/2× 字体均通过。标准/大字截图 SHA-256 为 `31e18380480c2b6976f6e776a843a46408d9691badb70bacaee84bb13841964e` / `95c0f8ae92d064cc11af4209d62d4a4a6756a3a67d53dba467b4dcaa280407d9`，位于忽略目录 `.tooling/saved-state-audit/`，仅属本地工程证据。当前源码重新安装后的完整 App instrumentation 14/14；Android JVM 180/180（Domain 56、App 46、Data 78），App/Data Lint 均 0 error，Debug、R8 Release、源码门禁和差异检查通过。Debug/未签名 Release APK SHA-256 为 `1b70a08445a3ae2805298612b91cf17549b85c7c7d287b89553945178c501e19` / `5637bcbe97b9aa073eb185875e216a01b4252fe6c917efe65f2f5994cab3797d`。外部 Beta 证据状态不变，发布仍为 `NO_GO`。
- 2026-07-25 收藏页已从“每日首页的另一种过滤状态”收口为独立、聚焦的知识归档入口。空状态文案与卡片当前真实操作统一为“收藏”，说明收藏后会在此出现，并提供“查看每日卡片”主操作；收藏页不再混入自动分析状态、推荐偏好和隐私中心。按钮与 Android 系统返回键都会回到每日卡片，且保留每日页自身的滚动与设置边界。改动只位于 Compose 展示/导航层，现有 Room 收藏、反馈和同步语义未变。新增 API 34 设备测试使用真实 Room 清空态，验证空状态、设置隔离、CTA 返回、系统返回，并生成标准截图；840×1867/2× 字体复验标题、说明和操作完整可达。标准/大字截图 SHA-256 为 `892187f0b7dd1108e2f61c96a8d2bf7a77e12a2e89f5ce344e2165eedf3a844e` / `f686965d6f43916bd221db7fc396e8d753d01e8ace8fa2291110244bb2364ef8`，位于忽略目录 `.tooling/saved-state-audit/`，仅属本地工程证据。完整 App instrumentation 13/13；Android JVM 180/180（Domain 56、App 46、Data 78），App/Data Lint 均 0 error，Debug、R8 Release、源码门禁与差异检查通过。Debug/未签名 Release APK SHA-256 为 `13f2068d797b3f842e5d0c40d2f4cbbaf2d862c2c4e99cc30ab0605bff666579` / `a579b6f0e164fe79b411edb93e9a2440f66c5b5ed8d93c0e9a8e23709fa755de`。本轮不改变外部 Beta 证据状态，发布仍为 `NO_GO`。
- 2026-07-25 “仅选择照片”用户完成引导后的首日空状态已从权限说明页改为可执行的选图起点。主卡标题为“从一件日常物品开始”，先说明主体清楚、画面简单和本机隐私/质量筛选，再提供杯子与餐具、清洁工具、数码小物三类具体起步建议；“选择一张照片”成为唯一主操作，其他 App 分享入口作为次级补充，推荐偏好与隐私管理继续保持第二层。建议布局使用卡片真实净宽，标准净宽横排三项，净宽 `<300dp` 或字体 `>=1.5×` 才纵排；状态和布局判断仍是纯 app UI policy，未改 domain/data 业务边界。`PhotoAccessPresentationInstrumentedTest` 在 Picker-only 状态真实断言标题、三类建议、主操作并生成截图；滚动器从抽象无障碍滚动改为真实滑动手势，精确 320dp/2× 字体也能依次到达建议、选图和展开后的自动发现入口。标准截图 SHA-256 为 `e8242db314ace01beb39990a0bc62196d451778e3815d2e3bcc16901a8082ed4`，位于忽略目录 `.tooling/empty-state-audit/`，仅属本地工程证据。完整 API 34 App instrumentation 12/12；Android JVM 180/180（Domain 56、App 46、Data 78），App/Data Lint 均 0 error，Debug、R8 Release 与源码门禁通过。Debug/未签名 Release APK SHA-256 为 `a613574db83c8d5bfe4bc28d11dcf6732312966a899ed8fba851fe23327ed95a` / `51176de1a10542a0f6e944029b7f731c1b373fe8cc566d60210a9d870b0d14d7`。本轮不改变外部 Beta 证据状态，发布仍为 `NO_GO`。
- 2026-07-25 首次体验已从抽象色块升级为真实可感知的产品示例。第一页使用原创生成且无人物、无品牌、无文字的室内扫帚照片，明确标注“示例照片”和“识别到 · 扫帚”，并以问题“扫帚为什么用一束细长刷毛？”演示照片到知识的转化；WebP 仅 56 KiB，来源、处理命令、用途边界和 SHA-256 已记录在 `docs/ASSET_PROVENANCE.md`。第三页修正了把屏幕阈值误用于卡片净宽的问题：普通 411dp 手机现在用两列兴趣选项，使“自动发现”和“仅选择照片”同时可见；容器净宽 `<300dp` 或字体 `>=1.5×` 才纵排。换页会先清除焦点、等待新帧再回到顶部，避免从第二页底部进入第三页时停在内容中段。新增设备测试真实走完三页、验证示例语义、每页顶部、两个入口和截图；标准与精确 320dp/2× 均通过，完整 API 34 App instrumentation 12/12。Android JVM 179/179（Domain 56、App 45、Data 78），App/Data Lint 均 0 error，Debug 与 R8 Release、源码门禁通过。标准首屏/入口页截图 SHA-256 为 `2685b089cc01e11d29e890464d9c89e1ccd988c6db4efcfdd0a3568f9a28323c` / `848f72545dc25fb72a8759563db78e8053685b203ee479258a18757b30288bb0`；Debug/未签名 Release APK SHA-256 为 `bafccad6fa2d6a69cd7945d02a1ec6b825a2809a6fb8c236042877af931fdeb3` / `a58b082112193c163e3d6a0bee6959b730ac120835e784a9886a4e94b69c346b`。本轮不改变外部 Beta 证据状态，发布仍为 `NO_GO`。
- 2026-07-25 卡片反馈区已从“说明 + 两个并排按钮 + 两个全宽按钮”的问卷式布局收口为同一组四项选择。标准净宽使用 2×2 描边网格，只有反馈容器净宽低于 260dp 或字体达到 1.5× 才切为纵向；独立纯 UI 策略和边界测试避免复用主操作 340dp 阈值导致正常手机误判为窄屏。标题改为“这条知识怎么样？”，背景降为中性的 `surfaceVariant`；有意思/没意思、识错了、太私人四项同时可见，后果文案明确区分调准推荐、隐藏卡片、删除并停止分析。“太私人”仍保留二次确认，设备测试真实点击后验证确认框与“保留卡片”返回，业务提交语义和 domain/data 边界未改。API 34 标准 2×2 截图和精确 320dp/2× 纵排截图 SHA-256 分别为 `da30950353e546903d615acab68120e6f4dbe2e39c32f44a31802799ba6a5b76`、`942bd8f8557be17992bc33590be5aaafd9a637159897d35e37c697738b6e6c63`，位于忽略目录 `.tooling/card-experience-audit/`，仅属本地工程证据。全新 `-wipe-data` API 34 AVD 最终 App instrumentation 11/11；Android JVM 179/179（Domain 56、App 45、Data 78），Lint 0 issue，Debug、R8 Release 与源码门禁通过。Debug/未签名 Release APK SHA-256 为 `8ad3f758dbeb8db5fc352b6165574ce574d336fad8cf43b10a56005c6133b2b7` / `8f946d975b1b41425e2ac61dd9565e4ba464046784a2c4862627e6a745036fae`。外部 Beta 阻断不变，发布仍为 `NO_GO`。
- 2026-07-25 每日知识卡的阅读层级已再次用 API 34 真实渲染收口。卡片保留识别把握、推送原因和可点击来源，但将“从你的照片说起”改为更直接的“为什么推给你”，来源可见文案压缩为发布者、完整标题保留在无障碍描述；收藏和物品提醒从绿色主操作降为等权描边次级操作，标签缩为“收藏 / 物品提醒”，知识正文重新成为唯一视觉主角。标准 1080×2400 截图与精确 320dp/2× 字体截图均由设备测试生成，后者证明两个操作纵向可达且不再出现多行挤压；截图 SHA-256 分别为 `181a5abbea25ce0c15d707e1621a1954dc2bcf71c70259180241a61c906822f2`、`8b23e6d0d9de54f8d5d63d9d041aec05edd763eb4fd15e2e5b814282dd4394e8`，位于忽略目录 `.tooling/card-experience-audit/`，仅属本地工程证据。大字测试现会真实滑动确认识别、来源和收藏均可达；组件 Pin 测试从 Pixel Launcher 返回后使用真实坐标点击隐私管理，消除无障碍动作已返回成功但窗口焦点尚未稳定的竞态。全新 `-wipe-data` API 34 AVD 最终 App instrumentation 11/11，Android JVM 178/178（Domain 56、App 44、Data 78），Lint 0 issue，Debug 与 R8 Release 通过。Debug/未签名 Release APK SHA-256 为 `bdf7c425671def3fc7abcf24933ce7aedda7ec55ba54f9b10787645a2ed9647b` / `96a14430b77bc07ff1e7e3441396d3e2f83d8ee48a487e0b45acdf36f324a5bb`。外部 Beta 阻断不变，发布仍为 `NO_GO`。
- 2026-07-25 Qwen Provider 已用用户提供的北京百炼业务空间凭据和项目既有 CC0 自行车图片重新执行真实请求。首轮暴露两个非模拟问题：文档中的 pnpm `--` 会被旧解析器当作未知参数；临时省略付费护栏的诊断请求虽然返回完整 JSON，却因模型使用了非约定 boundingBox 字段而被严格 Schema 拒绝。当前验证器兼容 pnpm 分隔符，在任何联网前于内存移除 JPEG APP/COM 元数据并复检无残留、无尾随字节和结构完整性，原始元数据不落盘也不发送；Qwen JSON Mode 提示固定完整字段和 `{x,y,width,height}` 坐标形状，明确禁止 x1/x2、left/right、数组等替代字段，并按阿里云官方建议移除可能截断 JSON 的 `max_tokens`。修正后真实 `qwen3.6-flash-2026-04-16` 在 5.46 秒内返回 `bicycle / 自行车 / 0.98 / sensitiveFlags=[]`，严格 Schema 通过，且验证器继续证明单卡一次模型调用和服务端确定性标题。生产安全头请求仍为 `403 access_denied`，同业务空间普通模型探针为 200，机器诊断保持 `ai_safety_guardrails_not_authorized` 与所需角色 `AliyunServiceRoleForSFMAccessingCIP`；本地 fallback 不是发布证据。最终 TypeScript check/build、后端 113/113 基础测试（另 13 项 PostgreSQL 按环境 skipped）、源码护栏 `qwenStructuredContract=1 qwenVerifierPrivacy=1` 和差异检查通过。本轮未改 Android/API；真人内容审核、真实托管云、正式签名、OEM/TalkBack/卡片抽检/cohort 等阻断不变，Beta 仍为 `NO_GO`。
- 2026-07-25 知识卡与桌面组件的照片占位文案已从可能暗示“候选图片绝不离开设备”的“照片保留在本机 / 照片在本机”收口为事实性的“原图暂不可显示”。该文案由 App 与 Glance 共用常量，源码护栏会拒绝旧的本机绝对暗示；这不改变既有隐私边界，候选仍只在明确授权后压缩、去 EXIF 并短暂上传处理。组件同时处理旧缓存卡 `title == detectedObjectName` 的重复层级：高置信度标题已等于对象名时不再重复第二行；中等/较低把握或更丰富标题仍保留识别提示。API 34 Pixel Launcher 端到端测试现在真实 Pin 组件、回到桌面并断言“见微 · 今日”“原图暂不可显示”可见且“自行车”只出现一次，随后完成安装态管理入口验证；最终 App instrumentation 11/11、Android JVM 178/178（Domain 56、App 44、Data 78）、Debug/Release Lint 0 error/42 warning、Debug 与 R8 Release、源码护栏和差异检查全部通过。Debug/未签名 Release APK SHA-256 为 `c6e058a75b9202d93e291bfaed1db3c3d789015d2a80b7fb4c53fb2837c2edc7` / `b22a7ee1dc9438f2be67095699eeb5edf86452ba67e12501a3b6b22675090207`；真实模拟器组件截图位于忽略目录 `.tooling/widget-experience-audit/widget-content-deduplicated-final.png`，SHA-256 `8821da053e0b0731d5a1480998a7c1ab2f1730919f636f5ab62116df08b89f06`，仅属本地工程证据。真人内容审核、真实托管云、正式签名、OEM 七天运行、真人 TalkBack、200 卡抽检和 cohort 阻断不变，Beta 仍为 `NO_GO`。
- 2026-07-25 卡片生成已移除第二次、仅为标题服务的 Qwen/Kimi 调用。旧链路在视觉识别和审核事实命中后仍调用远端 `CardWriter`，其超时、Schema 异常或 ID 回传错误会让已经具备可靠正文和来源的卡片整体失败，同时近乎翻倍单卡模型请求数。当前模型只负责一次视觉识别；服务端以审核目录对象名和 `factId` 稳定选择三种不增加事实的安全标题模板，30 字封顶，置信度低于 0.72 时仍由确定性策略覆盖为“这可能是…”。正文、`factId` 和来源直接来自已审核目录，不再发送给模型或接受模型回传；Qwen/Kimi 标题 Writer、Schema、生产装配和无效注入边界已删除。Qwen 受控验证器现明确输出 `modelCallsPerCard=1` 与 `cardTitlePolicy=deterministic_server_side`。最终验证为 TypeScript check/build、后端 112/112 基础测试（另 13 项 PostgreSQL 测试按环境显式 skipped）、API 契约、源码护栏 `singleModelCallCardPipeline=1`、E2E 自测、内存仓储真实 TCP 闭环，以及隔离 PostgreSQL 17.10 的 13 次迁移、13/13 集成测试和带 `deterministicTitle=1` 的编译服务 TCP 闭环全部 GO；PostgreSQL 进程已停止。本轮未改 Android/API 结构，因此未重建 APK；真实 Qwen 生产护栏、真人知识审核、真实托管云、正式签名、OEM/cohort 等外部阻断不变，Beta 仍为 `NO_GO`。
- 2026-07-25 “识错了”已从只记一条零权重事件升级为端云一致的终止状态。此前用户反馈识错后，错误卡仍留在每日页/收藏/组件，下一次同步还能用服务端 `scheduled` 行复活；若此前收藏，还会继续保留错误主题的 +0.5 兴趣信号。当前 Room 在同一事务写反馈状态/outbox、将卡片归档、撤销该卡 SAVE 权重、删除收藏，并把现有提醒改为待取消；ViewModel 同时取消本机 Work。Repository 在下载卡片前先确认 WRONG_OBJECT，未确认 outbox 保持为显示屏障，完整分页提交时强制同卡为 `archived`，因此陈旧页面和进程崩溃都不能复活；仅同步到 archived 卡也不会误记首卡可用。服务端以每设备/卡片统一 advisory lock 串行普通反馈与隐私删除，WRONG_OBJECT 会归档卡片、撤销该卡既有 LIKE/DISLIKE/SAVE 对主题权重的影响，并把后续陈旧 LIKE/SAVE 解析为已存在的终止反馈。交互文案明确区分“有意思/没意思改进推荐”和“识错了隐藏卡片”。源码护栏新增 `wrongObjectTerminal=1`。最终验证为后端 115/115 基础测试、真实 PostgreSQL 17.10 的 13/13 集成测试与 TCP E2E、Android 43 个 JVM 套件 176/176、API 34 Data 58/58 + App 11/11，最终反馈类 15/15 重跑；App/Data Debug/Release Lint 分别 0 error/42 warning、0 error/22 warning，Debug 与 R8 Release 构建通过。Debug/未签名 Release APK SHA-256 为 `ad37b102062814e489de03cc86642e7c1832a6dbd09a039eee8ea8438407d8f1` / `b80cb1db74bc5e78d75ab9480c4d8f830e452e15fdab1bc4ea366d1073cf8c40`。测试包、模拟器、ADB、QEMU 和 PostgreSQL 均无残留；本轮仍不构成真实云、正式签名、真人审核/OEM/cohort 证据，Beta 保持 `NO_GO`。
- 2026-07-25 卡片对象身份已从“混用模型原始叫法与审核目录主题名”收口为单一可信命名。视觉模型仍负责主题候选和置信度，但匹配到知识目录后，标题生成输入、低置信度“这可能是…”、`detectedObjectName`、推送理由和审核事实全部使用目录的 `topic.displayName`；目录对象名同时被约束为清理首尾空白后的 1–60 字，保持与 API/PostgreSQL 边界一致。端到端回归模拟模型返回“清扫刷”、目录命中“扫帚”且置信度 0.68，最终卡片只出现“扫帚”并保留不确定措辞。拍摄日期入口继续由 Zod 拒绝不存在的公历日期，历史任务读取层新增防御，`2026-02-31` 不会进入“为什么推给你”文案。最终验证为后端 114/114 基础测试（另 13 项 PostgreSQL 集成测试按环境显式 skipped）、TypeScript check/build、API 契约 13/8/1/9、真实回环 TCP 全闭环及源码护栏 `canonicalCardIdentity=1 strictCapturedAtBucket=1` 全部 GO。本轮未改 Android/API 结构，未重建 APK；真实 Qwen、真人内容审核与 200 卡抽检仍缺，Beta 保持 `NO_GO`。
- 2026-07-25 照片供给已把“每天随机挑一张”和“先理解一批、每天展示一张”合并为可靠的混合策略，而不是暴露一个受 Android 后台/联网状态影响、可能当天断卡的模式开关。自动发现仍在卡池低于 7 张时向 14 张补足，保证组件离线连续展示；`CandidateRanker` 先按画质、90 天时效、三项显式兴趣和本机反馈计算完整分数，再使用补卡当天的中国日历日期作为稳定种子，只在分差不超过 0.04 的最多 3 张候选内调整顺序。相同日期重试与进程重启顺序一致，不同日期可得到不同首选，明显低质或被负反馈降权的照片不能靠随机上位；自动首次筛选和后续 DEFERRED 补卡共用该策略，Picker/分享传空种子并保持质量优先。源码护栏新增 `qualityBoundedSerendipity=1`。最终验证为 Android JVM 175/175、API 34 DeferredCandidateSelector Room 2/2、App Debug/Release Lint 0 error/42 warning、Data Debug/Release Lint 0 error/22 warning、Debug 与 R8 Release 构建通过；Debug/未签名 Release APK SHA-256 为 `49cba45ffb0985b4fff2df97618ca3e0c606090d4ee485f874d5a0478b80084e` / `56defed88fbcc75543d58b481225b66c6e18808c60999cc1570d3d795c8f26ff`。测试包无残留，模拟器与 ADB 已关闭；真实用户是否感到“有惊喜但不离谱”仍需 cohort 的有意思/识错/没意思反馈验证，Beta 仍为 `NO_GO`。
- 2026-07-25 自动相册发现与主动选图/分享已从“共用一份最后写入者获胜的分析状态”收口为两条独立、可恢复的产品管线。旧实现中自动扫描、Picker 和 Sharesheet 的 WorkManager/Worker 会互相覆盖 `QUEUED/FILTERING/SYNCING/READY/FAILED`；两种模式并发时，主动结果页可能显示自动扫描阶段，自动补卡也可能被主动任务的终态遮掉。当前纯 Kotlin domain 定义 `AUTOMATIC_DISCOVERY` / `EXPLICIT_IMPORT` 作用域和仓储契约，data 层按来源路由并以不同 SharedPreferences 键持久化，ViewModel 在有待处理主动照片时只把主动作用域交给结果解析和进度卡，完成后恢复自动作用域展示。旧版无作用域状态只迁移到自动发现；非法 Work 输入会把两个作用域同时置为失败，避免留下陈旧“正在处理”。源码护栏新增 `analysisProgressScopeIsolation=1`。最终验证为 Android JVM 172/172、API 34 Data 56/56、App 11/11、App Debug/Release Lint 0 error/42 warning、Data Debug/Release Lint 0 error/22 warning、Debug 与 R8 Release 构建通过；Debug/未签名 Release APK SHA-256 为 `619827a4ecc562739a8a713b46e07b85118ecdb06dfb599948c040e443bfeea0` / `3e45c6d66ef0d3c6b496116e799f7efb9cda870a2f2c38e0bbf90b7688b850e6`。测试包无残留，模拟器与 ADB 已关闭；这是参考设备上的并发状态正确性证据，不替代真实 Qwen 时延、OEM 后台或 cohort，Beta 仍为 `NO_GO`。
- 2026-07-25 百炼生产护栏阻断已从泛化 `403` 收口为可机器判读的运维结果。Qwen Provider 当前生产请求与阿里云官方规范一致，固定发送 `X-DashScope-DataInspection: {"input":"cip","output":"cip"}`，运行时继续失败关闭；预检在“带护栏请求为 `403 access_denied`、同业务空间普通模型探针为 200”时输出 `ai_safety_guardrails_not_authorized`、必需请求头、`AliyunServiceRoleForSFMAccessingCIP` 和主账号下一步，不再把它误报成 API Key 或模型权限失败。部署说明已区分 Bailian 访问内容安全角色与 Guardrails 日志投递角色，明确 AI 安全护栏是独立按量付费服务，必须同时完成服务开通和当前业务空间的内容安全授权；本轮未代替账号执行付费开通或 IAM 授权。后端完整测试 111/111、TypeScript check/build 和 `git diff --check` 通过；真实生产请求仍须在控制台授权后重跑，当前 Beta 保持 `NO_GO`。
- 2026-07-25 自动候选池已从“只按拍摄日期分散、可由同一物件类别占满”扩展为日期与本地类别双重软多样性。后端原有策略只会在已经识别为同一 canonical topic 后轮换不同事实，不能阻止 12 张杯子或自行车照片先进入同一滚动池。当前 `CandidateRanker` 使用 bundled ML Kit 前两个经规范化、去重的有效标签形成本地类别签名；第一遍每个标签组优先最多取 2 张，并与“每个拍摄日优先最多 2 张”同时生效。只有一个泛化标签时不创建类别签名，避免 `object` 等弱标签误杀；如果可用照片确实只有一个日期或类别，第二遍仍按原排名回填到请求数量，并再次检查感知哈希近重复。纯 Kotlin 回归证明低分其他类别会先于同类第 3/4 张入选，且单类别仍能填满 5 张；API 34 Room 定向 2/2 证明反馈重排与同日 12 张补满未回归。完整验证为 Android JVM 170/170（Domain 54、Data 76、App 40）、Debug/Release Lint 均 0 error/42 warning、Debug 与 R8 Release 构建、源码护栏通过；后端未改，沿用连续工作链的 110/110 与 TypeScript check。Debug/未签名 Release APK SHA-256 为 `d85f2f7632f96e35d16ef11db32ceb293eb24ed2f3863f7c4948132dff70772f` / `b7f40bff9662652033c3d4ba38aaf4afa870c012eff17256336e4d2112c8721d`。模拟器已关闭；真实跨主题比例仍需由 Qwen 生产结果和 cohort 卡片抽检测量，Beta 外部阻断不变。
- 2026-07-25 自动隐私分析已从“三个 WorkManager 唯一链各自有序、跨链仍可重复读取同一 DISCOVERED 队列”收口为按来源有界串行。首次扫描、权限范围对账和每日补卡使用不同 unique work 名，旧实现可同时进入 `PrivacyScanWorker`，对同一批照片重复执行 bundled ML Kit 人脸、OCR、清晰度和标签分析并竞争更新状态。新增进程级 `PrivacyExecutionGate`：只有 `MEDIA_STORE` 自动范围进入共享可取消 Mutex；Picker/分享的 `EXPLICIT_IMPORT` 不进入该锁，用户主动操作不会排在一轮自动分析之后。20 路并发测试证明自动批次最大同时执行数为 1；另验证自动持锁期间显式导入立即进入、等待中的自动任务取消后不会执行且锁仍可复用。Worker 在等待后重新检查暂停、卡池和 Room 队列，避免用等待前状态继续。完整验证为 Android JVM 168/168（Domain 52、Data 76、App 40）、API 34 Data 54/54、Debug/Release Lint 均 0 error/42 warning、Debug 与 R8 Release 构建、源码护栏通过；后端未改，沿用同一连续工作链的 110/110 与 TypeScript check。Debug/未签名 Release APK SHA-256 为 `2a9a060619b0ef2c7884b70c6685b83ac3d21632707d7074bb0fb79bbd803946` / `38d31fd48e7952a5b14649b8a8bd3f70630c04d374a1c2df696b19746fb6798b`。模拟器已关闭；进程重启后的 Work 仍依赖 Room 状态幂等恢复，Beta 外部阻断不变。
- 2026-07-25 自动发现的卡池低水位闸门已从 Upload 末端前移到端侧隐私分析之前。旧每日链固定执行 `MediaStore scan → bundled ML Kit 人脸/OCR/清晰度/标签 → upload`，即使本地已经有 7–14 张可展示卡，也要先分析一批照片后才由 UploadWorker 发现无需补卡。当前仍保留轻量 MediaStore 增量索引，但 `PrivacyScanWorker` 在自动范围读取 Room 当前/未来 scheduled 卡数量：达到 7 张即直接发布 READY 状态并跳过 ML Kit；新照片继续以 `DISCOVERED` 留在本机，卡池跌破低水位后再处理。Picker/分享属于显式用户请求，任何卡池数量都不会被该闸门阻止。纯 Kotlin `shouldRunPrivacyBatch` 覆盖 6/7/14 张边界和显式导入；Hilt Worker 增加 CardDao 只读依赖。完整验证为 Android JVM 165/165（Domain 52、Data 73、App 40）、API 34 Data 54/54、Debug/Release Lint 均 0 error/42 warning、Debug 与 R8 Release 构建、源码护栏通过；后端未改，沿用同一连续工作链的 110/110 与 TypeScript check。Debug/未签名 Release APK SHA-256 为 `fe02626337517754e8576cd60091c8abc3dbec4733a49b6d6e7c19bbba7c82c6` / `a24c6ec766ffc7acef79486153f170263e2f0a6b4ce7851a0957149548baa234`。模拟器已关闭；这是本地调度与耗电上界证据，不替代 OEM 七天后台功耗测试，Beta 保持 `NO_GO`。
- 2026-07-25 首卡与主动选图结果已从“等待整批最多 12 张全部完成后才同步”收口为一次性快速发布。旧 `UploadWorker` 即使第一张候选已经由服务端完成，也会继续串行等待同批其余模型调用，最后才 `syncCards()`，直接把首卡 P50/P95 和主动选图反馈时间绑定到整批最慢项。当前 Worker 先完成原有启动同步，再以 Room 全卡计数区分真正首次安装：自动发现仅在本地从未有卡时，于第一个 `completed` 候选后立即同步一次；Picker/分享每批也在第一个成功候选后立即同步一次；已有卡的常规自动补充仍只做原有批尾同步。纯 Kotlin `shouldSyncCardsImmediately` 失败关闭地约束“只在完成候选后、每轮最多一次”，Room 新增只读 `countCards()`，源码门禁输出 `FIRST_CARD_DELIVERY_GATE=GO automaticFirstInstallImmediateSync=1 explicitImportImmediateSync=1 routineRefillBatchSync=1`。完整验证为 Android JVM 164/164（Domain 51、Data 73、App 40）、API 34 Data 54/54、Debug/Release Lint 均 0 error/42 warning、Debug 与 R8 Release 构建；后端未改，沿用同一连续工作链已通过的 110/110 与 TypeScript check。Debug/未签名 Release APK SHA-256 为 `d36bc86f785dbb61d996bda3914a23544d020c74944d31ecdec4a54517fbc83c` / `41b6f0b14dc56f635a935415a3ab3acbe7d359d3da27afe83ebbead3679c48c8`。模拟器已关闭；这证明同步时序与请求上界，不是实体机真实 Qwen 首卡时延证据，Beta 仍为 `NO_GO`。
- 2026-07-25 卡池候选的拍摄日期约束已从“同一天硬截断 2 张”改为“优先跨日期，再按排名回填”。旧排序在单次调用只有同日照片时只返回 2 张；`UploadWorker` 虽会重复提升延后候选并最终补足，却会绕过原多样性意图、制造多轮 Room 往返。现在 `CandidateRanker` 第一遍每个中国自然日优先取最多 2 张，先让其他日期进入结果；若仍未达到请求数量，再从同日溢出池补齐，近重复规则在回填时重新检查。纯 Kotlin 回归同时证明“高分同日照片不能挤掉可用跨日照片”和“只有同日照片时仍可填满 12 张”；API 34 真实 Room 定向 2/2 证明 12 张同日 DEFERRED 可在一次 promotion 中全部变为 READY。最终 Android JVM 162/162（Domain 49、Data 73、App 40）、Debug/Release Lint 均 0 error/42 warning、Debug 与 R8 Release 构建、源码护栏均通过；后端代码未改，沿用紧邻前一提交已通过的 110/110 与 TypeScript check。Debug/未签名 Release APK SHA-256 为 `c21663e3daefe6a0d55982a9fd8b9db0f4cad99352bf8299632dd86fc2e294fe` / `363a27ac5cb6f121e88aae8d9f444f398ec250863ad6333d72ad718ba7c57af8`。模拟器已关闭；这是排序与本地供给证据，不替代真实云识别成功率或 cohort 卡片多样性，Beta 仍为 `NO_GO`。
- 2026-07-25 首卡快速批次进一步从“12 张清晰且非敏感即停止”收口为“12 张相对历史库与本批次均不重复的合格候选才停止”。旧流程在后置去重前结束，12 张连拍可能只剩 1 张，甚至全部命中历史照片而无法支撑首卡；现在 `PrivacyScanWorker` 在每次 READY 后复用同一个 `CandidateRanker` 感知哈希规则，实时以历史基线和本批次计算唯一合格数量，未达 12 张则继续检查，但自动模式仍严格不超过 24 张，主动导入仍完整检查最多 20 张。纯 Kotlin 策略与去重测试覆盖历史重复、批内连拍和上限；源码护栏输出 `PRIVACY_QUEUE_GATE=GO originIsolation=1 firstCardUniqueEligibleTarget=12 automaticInspectionCap=24 explicitInspectionCap=20`。完整验证为 Android JVM 161/161（Domain 48、Data 73、App 40）、API 34 设备测试 64/64（Data 53、App 11）、Debug/Release Lint 均 0 error/42 warning、Debug 与 R8 Release 构建、后端 110/110 和 TypeScript check。Debug/未签名 Release APK SHA-256 为 `f3af590e5130ce265bfe77854fae17b92420096e878f0255ce58e6ffb1488c57` / `92d6679cd011fa49c9fef94799149418336909ea55f969c72082b82dd3d47dd6`。模拟器测试后无见微包或组件绑定残留并已关闭；以上仍是本地工程证据，不替代实体机首卡 P50/P95 或 Beta cohort，发布结论保持 `NO_GO`。
- 2026-07-25 首卡端侧筛选已从“自动与主动共用无范围队列、最多串行分析 60 张后才进入上传”收口为来源隔离的快速批次。`PrivacyScanWorker` 现在失败关闭地要求 `MEDIA_STORE` 或 `EXPLICIT_IMPORT` 范围；Room 在 `DISCOVERED` 阶段即隔离自动相册与 Picker/分享候选，自动链、权限对账、每日补卡和主动导入四个入口都通过同一 WorkRequest 构造器绑定 input 与非敏感 tag。纯 Kotlin `PrivacyBatchPlan` 规定自动发现最多检查 24 张，取得 12 张本机合格候选即进入排名/上传；主动导入则完整检查用户本次接受的最多 20 张，不会被大量自动相册候选饿死。API 34 Room/WorkManager 定向 7/7、真实系统分享 1/1、完整 Data 53/53、App 11/11、Android JVM 160/160、双 Lint 0 error、Debug/R8 Release、后端 110/110、TypeScript check 和源码护栏 `PRIVACY_QUEUE_GATE=GO` 全部通过。Debug/未签名 Release APK SHA-256 为 `eb5e88466a7c93672a501c907ef2752c9717ae7e994ed7005c535ec6639657db` / `5257796fc2ceb401874961e8108f838e8aec37680809d02d29be081d8d3c6f10`。这证明了端侧工作量上界和来源隔离，但不是实体机 P50<90 秒的 cohort 性能证据；测试结束已确认包、组件绑定和模拟器均无残留。Beta 仍为 `NO_GO`。
- 2026-07-25 桌面组件添加引导已从“每次看到第一张卡都重复催促”收口为真实安装完成态。`MainActivity` 现在只读取 `AppWidgetManager` 的实际绑定实例，并在 `onResume` 与系统 Pin 弹窗归还焦点时刷新 Compose 状态；未安装时第一张每日卡后保留 CTA，已安装后 CTA 自动消失，隐私中心仍提供“再添加一个桌面组件”，取消系统弹窗则继续保留原引导。判断与文案位于纯 Kotlin `DiscoveryUiPolicy`，Activity 只负责框架状态。新增 API 34 Pixel Launcher 端到端测试从真实卡片点击 CTA、确认原生 “Add to home screen”、验证 provider 绑定、返回 App 并读取完成态管理文案；完整 App 11/11、Android JVM 158/158、Debug/Release Lint 0 error、Debug 与 R8 Release、后端 110/110、TypeScript check 和源码护栏 `widgetInstallCompletion=1` 全部通过。Debug/未签名 Release APK SHA-256 为 `9afec42265a920047e630871075146eeb7f2262eed1f68215061c9b132bc8098` / `279d18ba6c61bf383db48736b95af6d16ec2dc26312cdc3c02446a389cd0b132`；视觉证据位于忽略目录 `.tooling/card-experience-audit/widget-install-completion.png`，SHA-256 `574fb86fe4c9db6d52d0f3b2efd44beeb82fbcd7195cc240985ce1300c8c0dfe`，只属模拟器工程证据。测试结束已确认包、组件绑定和模拟器均无残留；Data 51/51 设备回归来自紧邻上一提交，本轮 App-only 变更未重复执行。Beta 仍为 `NO_GO`。
- 2026-07-25 两种取图方式已从“首次体验的一次性选择”补成可逆的长期产品设置。此前用户选择“仅选择照片”或首次拒绝权限后，App 内没有重新开启自动发现的入口；现在折叠隐私中心会按真实系统权限显示三态：picker-only 提供绿色主操作“开启自动发现”，Android 14 部分授权提供次级“调整可访问照片”，完整授权只显示已开启而不重复索权。`ConfigurePhotoAccessUseCase` 置于纯 Kotlin domain：重新开启会同时安排首轮扫描与每日补卡；回到 picker-only 会等待并取消所有自动发现链，显式 Picker/分享导入不受影响；暂停时只更新范围并给出恢复后生效的诚实文案。API 34 真实系统 Permission Controller 已验证手动模式入口确实弹出照片授权；另以独立进程分别授予 `READ_MEDIA_VISUAL_USER_SELECTED` 和 `READ_MEDIA_IMAGES`，验证部分/完整 UI 与 `currentPhotoAccess` 一致。最终 App 10/10、Data 51/51 设备测试，Android domain/data/app JVM、双 Lint、Debug APK、后端 110/110、TypeScript check 和源码护栏 `reversibleDiscoveryControl=1` 全部通过。Debug APK SHA-256 为 `281d46477d6b9c943637ce37e5f5912b326e5202bbfe7dc1eb6925fc7c9adaee`；视觉证据位于忽略目录 `.tooling/card-experience-audit/automatic-discovery-control-final.png`，SHA-256 `81da811bf9b3b9ccfab17ce85d6b87aa014edb042c52a928b60210cd20e15c3d`，只属模拟器工程证据。Beta 仍为 `NO_GO`。
- 2026-07-25 首页首屏已从权限设置语气收口为消费产品语气：品牌副标题改为“从你的照片里，每天认识一件小事”，不再把“照片权限”放在标题下；权限范围以行为结果（全部授权照片、仅选中照片、仅主动选择/分享）移入折叠的“你的数据与隐私”摘要。已有卡片时若分析被暂停，首页会独立显示可恢复提示，避免状态被缓存卡片掩盖；无卡片时仍沿用原有可操作空态，未改变权限或扫描边界。纯策略测试覆盖三种权限与暂停可见性，API 34 真实 Activity/Room/Compose 在 411dp 与 320dp 均验证品牌、正文→识别→照片缘由层级、紧凑页签和旧技术文案消失；最终 App 8/8、Data 51/51 设备测试，Android JVM、双 Lint、Debug APK、后端 110/110、TypeScript check 和源码护栏全部通过。Debug APK SHA-256 为 `c573c1c8be6cf4981bd786ed79a2b9e8ae4d5f2214042fc98e09f423e771cf5a`；默认/320dp 截图位于忽略目录 `.tooling/card-experience-audit/knowledge-card-home-final.png` 与 `knowledge-card-home-320dp.png`，SHA-256 分别为 `4e18965df1182ea6af62ce193c4782cf32e0e9e57f36da257b5dc309e66426a7`、`f442a35ca14fd4d72d7dc6798dfe7a515e2c2909e50327faaa28c20b2074f9f7`，只属模拟器工程证据。Beta 仍为 `NO_GO`。
- 2026-07-25 卡片反馈已从“只影响下一批新照片”补成真实的后续补卡学习闭环。`CandidateRanker` 统一折叠空格、下划线、连字符和斜杠，因此 `traffic_light` 可正确匹配 ML Kit 的 `Traffic light`；卡池不足而恢复本机 `DEFERRED` 候选时，不再按质量分直接批量置 READY，而是在最多 100 张有界池中重新读取用户当前 3 项显式兴趣和最新本机主题反馈，综合质量、时间、兴趣、反馈与日期多样性后只提升下一批最多 12 张。Picker/分享与 MediaStore 的来源隔离仍由 DAO 查询强制。新增真实 Room/API 34 回归证明低质量但正反馈匹配的多词主题会排到高质量无关物件之前，未选候选继续保持 DEFERRED；完整 App 8/8、Data 51/51 设备测试，Android domain/data/app JVM、双 Lint、Debug APK、源码护栏和差异检查均通过。Debug APK SHA-256 为 `2a880fe409c9b37a37f3a81fc186ba3a8e786c4c7033314f63a9418e6efb527a`；Beta 仍为 `NO_GO`。
- 2026-07-25 每日卡从“识别结果展示”推进为“知识优先的每日发现”：服务端会读取当前匿名设备同主题最近 4 张卡，优先选择尚未出现的真人审核事实；可选事实全部看过后才回到最久未出现的一条。该策略同时落在 InMemory/PostgreSQL 仓储，PostgreSQL 17.10 的 13/13 集成测试与 TCP E2E 均为 GO。照片缘由改为可验证的自然语言（拍摄日期、规范对象名、主动授权来源），不加入位置或模型臆断。Android 将正文移到识别置信度之前，把“今日识物”改为“今日一知”，并把照片缘由和来源合并为“从你的照片说起”。新增 API 34 仪器测试验证正文→识别依据→照片缘由的实际屏幕顺序并输出真实截图；最终 App 8/8、Data 50/50 设备测试，后端 110/110 基础测试、13/13 PostgreSQL、Android JVM、Lint、Debug APK、源码护栏和差异检查全部通过。Debug APK SHA-256 为 `b1c5a5b45af67fa8f19ec7aef5b998f92beaf2e6250e395d756ab542bcc1c6d2`；截图位于忽略目录 `.tooling/card-experience-audit/knowledge-card-hierarchy-final.png`，SHA-256 `ccf23fc4b180cc795f22a2730a86ee39e5aa7fdff5bdb395726cf4c3fea12117`，只属模拟器工程证据。Beta 仍为 `NO_GO`。
- 2026-07-25 主动选图/分享已从“只提示排队、结果可能被未来卡池隐藏”补成可恢复的直接结果闭环。`PhotoImportOutcome` 返回本机随机候选令牌；App 只在设备私有 `pending_import_result` 中持久化最多 20 个不透明令牌、结果卡 ID 和 NO_MATCH/FAILED 状态，不保存 URI、文件名、标签、对象名或模型输出。进度页明确本机隐私筛选和可靠事实匹配；卡生成后即使排在未来日期也会直接打开精准卡片，关闭 Activity 后重新进入仍恢复；无命中和失败改为持久、可操作的专属结果卡，不再与后台全局状态互相矛盾。清本地索引和删云端数据会同步清除此状态。真实 API 34 Photo Picker 用非个人截图夹具完成选择、私有导入、过滤和“换一张照片/收起”结果页；新增设备测试证明未来卡即时打开、跨新 Activity 会话恢复、返回每日页后清除焦点。完整 App 7/7、Data 50/50 设备测试，domain/data/app JVM、Debug APK、Lint、源码隐私护栏和差异检查均通过。Debug APK SHA-256 为 `ccaf895b58e700a0d31a0467b84888fb808988da2b2a89b1347c44d448316877`；视觉证据位于忽略目录 `.tooling/import-result-flow-audit/final-result.png`，只属模拟器工程证据。Beta 仍为 `NO_GO`。
- 2026-07-25 已使用下载的百炼按量付费业务空间 CSV 做受控 Qwen 诊断，凭据只在进程内读取，未写入仓库、`.env`、日志或证据。北京业务空间端点、固定 `qwen3.6-flash-2026-04-16` 模型、真实图片结构化识别和受约束标题生成均成功；生产请求携带 `X-DashScope-DataInspection` 时返回 `403 access_denied`，而无该可选付费护栏的访问探针为 200，因此当前明确阻塞在同业务空间尚未开通 AI 安全护栏及服务关联角色授权。生产 Provider 仍默认强制护栏，只有 `verify:qwen-provider` 可显式执行失败关闭的本地诊断。Android 卡片供给改为混合滚动池：自动发现仅在未来缓存低于 7 张时启动，并向 14 张补足、每轮最多分析 24 个候选；用户 Photo Picker/分享导入不再被健康自动缓存阻塞，每轮最多处理 20 个显式候选。后端 107/107 基础测试、TypeScript check/build、源码密钥护栏，Android domain/data/app JVM 测试、Debug APK 和 Lint 全部通过；当前 Debug APK SHA-256 为 `1f16c6c79135e9554369d4292107bdf01c7161aa3f1a834e84f4e10a39bdb119`。当前实现基线 HEAD 仍为 `77ce415`，本轮改动尚未提交，Beta 结论仍为 `NO_GO`。
- 2026-07-24 外部发布状态已从当前工作区重新核对，而不是沿用旧结论：`check-knowledge-readiness` 返回 200 个主题、0 ready topic、11 状态 approved、0 真人签注，并因缺少受保护 reviewer allowlist 和 200 个完整真人审核主题失败；`check-container-deployment-inputs` 返回镜像摘要、声明摘要和基础镜像固定均为 0；`check-beta-readiness` 因 `evaluation/beta-evidence.json` 不存在失败。`android/keystore.properties`、正式 `config/evidence-trust-policy.json`、云运行、正式 Release、证据装配文件均不存在，环境也没有可用的见微/Qwen/OSS 生产变量或 Release API 配置。仓库仍干净且最新实现提交为 `0530c42` / `80d43b8`；此时继续生成模拟配置或合成证据不会推进真实 Beta，必须取得真人审核身份、真实云、正式签名/信任材料或实体机 cohort 之一后再沿对应 runbook 执行。
- 2026-07-24 首卡时延从 UI “观察到卡片”改为真实落库事件：domain `FirstCardMetricRecorder` 作为无 Android 依赖端口，由 data `RoomCardRepository` 在完整分页通过且非空卡片批次成功 `upsertAll` 后调用；App `BetaMetricsStore` 幂等保留首值。首页不再补记晚到的观察时间，用户切后台或进程重启不会把等待下次打开 App 的时间误算成生成耗时；指标实现抛出异常时已提交卡片仍保留且同步成功。组件成功口径也确认来自 `AppWidgetManager.getAppWidgetIds()` 的真实实例，不来自 Pin 请求受理。API 34 新增两项 Data 设备回归并复跑 13 项卡片同步/隐私顺序测试；完整回归为 JVM 146/146、instrumentation 56/56（App 6、Data 50）、双 Lint 0 error/32 warning、Debug 与 R8 Release 成功，源码守卫输出 `FIRST_CARD_COMMIT_METRIC_GATE=GO`。`.tooling/truthful-beta-metrics-audit/audit.json` 更新为 schema 2、`GO`、`releaseEvidence=false`，SHA-256 `b39caf9435f0d1bcae1675ee6ae60fc488a3539271474c91b57c52895f4e4169`。Debug/未签名 Release/App/Data instrumentation APK SHA-256 为 `f18e3a6a5112708eeadf5089a3881f7cd0744f4f4e8adc2dffc8c456f93bab5d` / `f65d3de90abfea21141937834ec8674c547d56dc98fb939749a7c6b78fd27058` / `81e86ee8bacf3238503ec542300a55d3bbae727b80e14c6e7e66208af6aea119` / `b0d02628ac312790bb338066cd3ff771107f8ae0f138ca20868af95ad307634d`。真实 P50/P95、组件添加率和其他 cohort/发布证据仍未产生，Beta 保持 `NO_GO`。
- 2026-07-24 Beta 本地指标已从“所有偏好动作混作反馈”收口为可用于真实 cohort 判定的语义边界：domain `FeedbackAction.isCardFeedback()` 明确把 SAVE 排除在卡片反馈外；收藏仍调用 `markEngaged()`，但不再污染 LIKE/feedback 分母；组件或提醒的 card ID 只有成功解析为本地有效 `focusedCard` 后才记录点击互动。API 34 两项新设备测试证明 SAVE 被指标 API 拒绝、LIKE 独立计数、有效精准回卡写入 `firstEngagedAt`，且导出不含照片路径、来源 URI 或候选令牌。`.tooling/truthful-beta-metrics-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `28223cf0d8646df6309939244d018aa7308a5f9e18e025a9e8e2c939c57ec08a`。完整回归 38 个 JVM 套件 146/146、API 34 instrumentation 54/54（App 6、Data 48）、源码守卫 `truthfulBetaMetrics=1`、双 Lint 0 error/32 warning、Debug 与 R8 Release 成功；Debug/未签名 Release/App/Data instrumentation APK SHA-256 为 `7cd55769c8f5b017e4a99a52cbd48a258e6f6b467d0dcb14bfe584d3cf04555c` / `0a17ea45e1d8c4b93d3ccfc71d09e8b3b574b2e796f5508c7c91cc9f37e4b0b7` / `e51553e2e692cc1705513fa96a326055f643cdc6e4b907d8c28397b36ff8146c` / `281bb02afafe20dc49842294b7a4bd84257a333068b135a6f8d739e673fc8269`。实际检查仍因 `evaluation/beta-evidence.json` 缺失返回 `NO_GO`；真实 7 日互动率、LIKE 率、真人内容审核、真实云、正式签名、OEM 实体机、真人 TalkBack、200 卡抽检和 cohort 均未被本地证据替代。
- 2026-07-24 Android 分享导入已成为与系统 Photo Picker 一致的产品闭环：domain `ImportPhotosUseCase` 统一私有复制、暂停失败关闭和后台排队；`ShareReceiverActivity` 使用进程级 `UserOperationGate`，提供确认、明确进度、冲突/失败/不可读原地重试；`MainActivity` 采用 `singleTask` 并只接受已知、数量有界的结果通知。API 34 标准布局与 320dp/2× 字体均通过同一完整设备流程：操作冲突被拒绝、重试成功、暂停时私有副本存在但无分析 Work、来源 URI 未持久化、已有首页实例复用。`.tooling/shared-import-flow-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `2949ec0196268f59b36bd7276ce92c77bb1d53e184d72d0202aede4286c6eb8f`。完整回归 38 个 JVM 套件 145/145、API 34 instrumentation 52/52（App 4、Data 48）、源码守卫 `sharedImportFlow=1`、双 Lint 0 error/32 warning、Debug 与 R8 Release 成功；Debug/未签名 Release/App/Data instrumentation APK SHA-256 为 `7cf23e0700f88ff997cf2bec4aeaffebdffcf8f9900abdc57b773cd0b15fd3c6` / `1f83474c1148b48e6ef1c3d5d7a7c9b42cbdf24b0127ea306d0b8109c2e998c3` / `4979700deaf7cde4410d9f60e98e1d24eb7fc741bfe547a93ed26a2a239f1d6c` / `61b03063a0694a6492ddc78a297569d5660b6c51e4f17a540932b8e99f0b15e6`。真实云、正式签名、OEM 实体机、真人内容审核、真人 TalkBack、200 卡抽检和 cohort 仍缺，Beta 保持 `NO_GO`。
- 2026-07-24 用户可触发的收藏、反馈、提醒、扫描控制与删除操作现由 `UserOperationGate` 原子串行化；同步入口先占门再启动协程，错误操作不能释放活动门，取消仍按协程语义传播。Compose 在操作期间禁用所有冲突 mutation，并在顶部显示具体进度；照片后台分析继续使用独立“照片分析”语义。API 34 标准布局和 320dp/2× 字体均实际触发受控云端删除，确认“正在删除云端数据 / 操作进度”可见，导入、暂停、清索引及再次删除的父节点均 `enabled=false`，大字号确认对话框完整可操作。`.tooling/serialized-user-operations-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `60723b4b986bdabdecfcfeca7b7e8d4052b06424a1f3cc1a86efdc55a93ea011`。完整回归 JVM 139/139、API 34 instrumentation 51/51、源码守卫 `serializedUserOperations=1`、双 Lint 0 error/32 warning、R8 Release 成功；Debug/未签名 Release SHA-256 为 `99420560f24c5feafb4d779e8580c1333d1c7663316ab4b71f914627ced17deb` / `6b8f8189b3a4aa0462ebc511252300878ffa8e72435e548d0531bd5bfdc8e897`。真实云、正式签名、OEM 实体机、真人内容审核和 cohort 仍缺，Beta 保持 `NO_GO`。
- 2026-07-24 首页三种内容模式已拥有独立滚动状态：每日与收藏各自保留浏览位置，组件/提醒精准入口不再继承普通列表索引；每当 `focusedCardId` 变化，精准列表会回到“打开的知识卡 / 返回每日卡片”顶部，关闭后恢复原每日或收藏位置。API 34 无照片夹具从历史“拉链”位置打开未来“保温杯”，再从已滚动精准页切换“回形针”，两次都从顶部开始，关闭后恢复“拉链”；320dp/2× 字体重复验证顶部入口、未来日期和目标对象可达。审计 `.tooling/independent-home-scroll-audit/audit.json` 为 `GO`、非发布证据，SHA-256 `bc37eeda04b2bb3fdf0e1fcc21294d2059ce573e887e0b2d3dc3c164190ee77a`。完整回归 JVM 135/135、设备 51/51、源码守卫 `independentHomeScroll=1`、双 Lint 0 error/32 warning、R8 Release 成功；Debug/未签名 Release SHA-256 为 `3d747d4ae9f189772676f45282ea9b4691c937e928608896221007e839db212f` / `60ce0c7de70b7609976edb52d3c3134c70ccdf8ef70238cbccef3e16416bd928`。Beta 仍为 `NO_GO`。
- 2026-07-24 每日页已修复历史卡固定显示“今日识物”的产品语义错误。纯 Kotlin domain 规则按中国自然日生成今日、昨日、同年月日、跨年完整日期和未来计划日期；Compose 每日流增加“今天 / 往日”分区，收藏与精准入口复用同一日期语义，无障碍节点暴露“卡片日期”。API 34 无真实照片夹具实跑验证 `今天 + 今日识物`、`往日 + 昨日识物`、`2025年12月31日识物`，未来精准卡显示 `7月25日识物` 且不冒充今天；320dp/2× 下历史分区与日期可达，crash buffer 为空，显示、包和夹具已清理。审计 `.tooling/truthful-card-date-audit/audit.json` 为 `GO`、非发布证据，SHA-256 `f96a65ab64e11279b01ce6bb62e9466d8ae87fbdf6845362d3be4c399d092a34`。完整回归 JVM 135/135、设备 51/51、源码守卫 `truthfulCardDates=1`、双 Lint 0 error、R8 Release 成功；Debug/未签名 Release SHA-256 为 `eb0ba918e62818cd9a0ab806bbc3c4180c90b8ddbc3c386d0a8ba986332d0dcc` / `5f55d331f797f20b3bf3b117dc98551c8adddb4cea3519ceaefd6678cf005634`。Beta 仍为 `NO_GO`。
- 2026-07-24 组件/提醒精准回卡已从“把未来卡插到每日列表顶部”收口为独立入口模式：domain 分离普通今天/历史卡、合法目标和不可用目标；Compose 只展示目标卡及返回动作，系统返回先退出，失效 ID 明确显示不可用且不冒充另一张卡。API 34 无照片夹具验证普通页 `today=1/future=0`、合法未来深链 `future=1/today=0`、可见/系统返回恢复每日页、失效入口单一返回，以及 320dp/2× 下完整内容和操作可达。审计 `.tooling/focused-card-entry-audit/audit.json` 为 `GO`、非发布证据，SHA-256 `b3b99993502ae711ff5d5b4fcff52b93dde9f2f6d00b11a10ef2473003fe2009`。完整回归 JVM 130/130、设备 51/51、源码守卫 `focusedCardEntry=1`、双 Lint 0 error、R8 Release 成功；Debug/未签名 Release SHA-256 为 `9086669c452743106396cdce0ac6461c623a0151362ca38393c6efe45eb0ca1f` / `7cbae81c84038bbfb64142943f0bb7f0b009ec60a5391b3aefc64b8d4d789ba3`。Beta 仍为 `NO_GO`。
- 2026-07-24 引导期“三项兴趣”已升级为长期可控推荐偏好：domain 定义五类兴趣、恰好三项、默认回退和排名词，data 用兼容旧值的 Repository 持久化，ViewModel 提供状态/保存，`PrivacyScanWorker` 消费同一 Repository。首页可查看、替换、取消和保存，明确从下一批新照片开始影响排序且反馈不会暗改显式选择。API 34 标准宽度完成真实替换和私有偏好核验，强停冷启动保留；320dp/2× 下所有选项/操作可达，crash buffer 为空。完整回归 JVM 130/130、设备 51/51（App 3、Data 48）、源码守卫 `userInterestControl=1`、App 双 Lint 0 error、R8 Release 成功；Debug/未签名 Release SHA-256 为 `f239c5c2a1e77f3a108c552104db25cf7bc453b659f5188dfe217b830c8b33f3` / `66d535b157874b8ce6ab51ae85f91240685b05671213ac753959fd1306bbd7dd`。这些仍是模拟器与本地工程证据，Beta 保持 `NO_GO`。
- 2026-07-24 真人知识审核工作台已补齐可执行的批次操作层：每条事实即时显示就绪状态与缺失项，支持全部/待处理/已就绪筛选和“下一条待处理”，批次进度同时显示 ready/open；高风险批准必须至少存在并勾选两个 official/professional 来源，未全部就绪时完成按钮失败关闭。冲突或保存失败可导出仅供人工恢复的本地草稿，保留 decisions 但排除 CSRF，且不能直接应用目录。真实 Android 14 Chrome 正常流输出 `initialOpen=2 readyFilter=1 nextOpen=1 highRiskTwoSourceGate=1 completedReady=2 finalizeFlush=1 finalized=1 autoApply=0 runtimeExceptions=0`；不同内容并发写入输出 `localInputPreserved=1 competingContentPreserved=1 explicitReload=1 recoveryDraft=1 csrfExcluded=1 destructiveAutoReload=0 runtimeExceptions=0`。这仍是合成本地工具验证，不是 624 条真人审核，Beta 保持 `NO_GO`。
- 2026-07-24 真人知识审核工作台已修复会静默丢失责任人输入的三类状态问题：首次保存后控件不再闭包写入旧 decisions，对请求期间产生的新编辑会按 `editVersion` 补存，完成批次前必须刷新到无 dirty/in-flight；409 冲突保留本页输入并要求显式确认后才重载。真实 Android 14 Chrome 还发现外部 Intent 会在 `SameSite=Strict` 下消费一次性入口但丢 Cookie，现改为顶层导航兼容的 `SameSite=Lax`，POST 仍受回环 Host、Origin、CSRF 和一次性入口保护。真实 Chrome 合成回归通过两次自动保存、完成前最后一刻编辑、双来源健康事实最终写批次不自动应用，输出 `decisionIdentityPreserved=1 finalizeFlush=1 autoApply=0 runtimeExceptions=0`；并发内容版本回归输出 `localInputPreserved=1 explicitReload=1 destructiveAutoReload=0`，单来源健康批准仍被拒绝。这不构成真人语义审核，目录仍为 0 真人签注，Beta 仍为 `NO_GO`。
- 2026-07-24 知识来源审核准备链路已从“代理 DNS 全部失败”恢复为可执行状态：本机 VPN/代理把公开域名统一解析到保留的 `198.18.0.0/15` 假 IP，原安全检查因此正确失败关闭。新增显式 `--google-doh` 模式，只访问固定 `https://dns.google/resolve`、禁止重定向、核对 Question 与记录类型、只接收 A/AAAA；得到的地址仍必须通过公网校验，随后来源 HTTPS 连接钉扎该地址并继续以原域名做 TLS/SNI，DoH 返回私网地址的自测失败关闭。真实运行已完成状态批准候选 13/13、全部编辑来源 531/531 可达，证据 SHA-256 分别为 `dfdc08abd113e113532c675a93237b2cbf1dae80990ecd042f3df2f2eec0f7dd`、`718c7d19c8b0a6a488885f6ca52402a590e0988d748185a21fed0884cc74b7bb`。重新生成的审核队列绑定当前 `2026-07-19-beta.62` 目录和来源证据，覆盖 200 个主题、624 条待审事实，JSON/Markdown SHA-256 为 `bb4a0f50c4daa5038b0c7cbc24a8c75087dd68640b5b53388cc4960a13f06253`、`af92d6e9437b82194e33f14385e62a47d0c8a98b681919adb1a209c7e4d59e19`；首批 20 条工作台预检全部 `pending`，工作台对抗自测通过。审计 `.tooling/knowledge-source-results/review-readiness-audit.json` 明确 `humanAttestedFacts=0`、`semanticSupportVerified=false`、`releaseEvidence=false`，SHA-256 `9a411b498af63e5af423efadbd512891a00527067c7e11481e49d7d6e952fc01`。这只消除了审核执行前的网络阻塞，不把可达性冒充语义支持，Beta 仍为 `NO_GO`。
- 2026-07-24 物品提醒补齐“追踪存在但卡片已不存在”的失效边界：提醒发送资格不再由 Repository 先后读取多个状态，而是由 Room 单条 `SELECT EXISTS` 原子联结 `local_tracked_items` 与 `knowledge_cards`，并同时核对非删除同步状态、启用日和提醒周期；因此卡片被删除后，即使追踪行或陈旧 Work 仍残留，也会失败关闭而不发送一条点开无内容的通知。API 34 真实 WorkManager 正向证明“有效卡片 + 有效追踪”会通知且精准回卡；两个独立负向路径分别证明“只删卡片、保留追踪”和“恢复卡片、只删追踪”时，陈旧 Work 均到达 `SUCCEEDED` 但无通知。`.tooling/reminder-privacy-audit/audit.json` 保持 `physicalNotificationTap=false`、`releaseEvidence=false`，SHA-256 `123a12f1bacc6dbc911337c74a4bed46e03c15ff7da8c3b515fb041b8372a3b8`。完整回归 JVM 126/126、API 34 instrumentation 49/49（App 3、Data 46）、源码守卫新增 `reminderCardPresence=1`、App Debug/Release Lint 0 error（22/8 warning）、R8 Release 成功；Debug/未签名 Release/App/Data instrumentation APK SHA-256 分别为 `ddfa738b29a9dee0b823459be203b70620f96b2f5b9f99ba5a9d550c5f45b73a`、`1cebe1f4ddb82247e980381463406913a987a815816545938c3006adb1025df0`、`4ffa7b1dd437115cb057a3c96e1a29732d216e8c09ecffb8c00a3c06e9c2ea70`、`573511c75fc5e70ba6ddc72038c09badef43f41d83052399a4b4d86c4fc00344`。这仍不是实体机真人通知点击或正式 Release 证据，外部 Beta 阻断不变。
- 2026-07-24 物品提醒从“打开首页”补齐为精准回到被追踪卡片：通知 `PendingIntent` 复用组件已验证的 `MainActivity.EXTRA_CARD_ID` 协议，MainActivity 冷启动和复用时分别由 `onCreate`/`onNewIntent` 聚焦卡片，不恢复卡片标题到通知或 Work 输入。API 34 首次直接从后台测试进程发送 PendingIntent 被系统后台 Activity 启动规则拦截；最终测试显式使用 Android 14 允许的 `MODE_BACKGROUND_ACTIVITY_START_ALLOWED` 后，真实通知 PendingIntent 启动 MainActivity，并读回完全一致的 card ID。该边界记录在 `.tooling/reminder-privacy-audit/audit.json`，明确 `physicalNotificationTap=false`、`releaseEvidence=false`，SHA-256 `34aaecadc89d6cd5922e241d2547a78361fbf32ed4bcf8c7fa092adc4a2502f7`。完整回归仍为 JVM 126/126、API 34 instrumentation 49/49（App 3、Data 46）、源码守卫新增 `reminderCardDeepLink=1`、App Debug/Release Lint 0 error（23/9 warning）、R8 Release 成功；Debug/未签名 Release/App/Data instrumentation APK SHA-256 分别为 `ea717910e33d51dd6524241dae80846ce247766f1349c5d0e9481c7c26b28dcd`、`1c505805766d49337f8a83eba175af2e9626c58fc2e44bc293797e21c4d16302`、`1b3c26bf73a60dc66a07d4c4bf05c56a07ec9c3abf9cce80ee9e50583c021e50`、`e78678b22e71a8d91f85ae2b5e43aaf40c8ba55387673b3559bf58d6d3dbb151`。这仍不是实体机真人通知点击证据，外部 Beta 阻断不变。
- 2026-07-24 物品提醒补上隐私事务后的崩溃/陈旧 Work 防线。`ItemReminderWorker` 改为 Hilt 注入的 `CoroutineWorker`，发通知前重新读取 Room，只有 card ID、启用日、周期和非删除同步状态全部仍匹配才继续；“太私人”、取消提醒、云端删除、周期更新或进程崩溃留下的旧 Work 均失败关闭。提醒 Work 输入与通知正文同时移除卡片标题，极端“状态复核后、系统通知前”竞态也不会暴露私人物件名称。API 34 真实 WorkManager 正向证明有效追踪会通知，负向证明移除持久追踪后陈旧 Work 虽执行成功但不发通知，且通知 title/text/bigText 不含探针标题。`.tooling/reminder-privacy-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `1a17bd0072ff538a93aa772e2b40245197a378ef3a335fd8af83bad4ed7366a1`。完整回归 JVM 126/126、API 34 instrumentation 49/49（App 3、Data 46）、源码守卫 `reminderPrivacyGuard=1 genericReminderContent=1`、App Debug/Release Lint 0 error（23/9 warning）、R8 Release 成功；Debug/未签名 Release/App/Data instrumentation APK SHA-256 分别为 `55b85f5eb883a4c8408d72c10110dcba37d05523cc4184e099bc354eb09288bc`、`ee8a729592f8b12719d3200bb5885ff95b689befd9c759347eae325a0a4e63b4`、`d50b2e0536f5c295045a13112a5d7a26c3bd434ad4e3f0a11f1c5a2b060d7200`、`e78678b22e71a8d91f85ae2b5e43aaf40c8ba55387673b3559bf58d6d3dbb151`。该结果只证明本地工程防线，外部 Beta 阻断不变。
- 2026-07-24 卡片反馈已从“按钮点击后只弹一句反馈已记录”升级为可持久、幂等、可解释的产品闭环。Room 10 新增卡片级联的 `card_feedback_states`；一张卡只接受首个 LIKE/DISLIKE/WRONG_OBJECT，状态、同步 outbox 和本地主题权重在同一事务提交，重复或冲突选择不再重复训练，服务端确认 outbox 后可见状态仍保留。SAVE 的 outbox/权重也改为原子提交。“太私人”独立于普通反馈并要求二次确认；确认后同一事务撤销该卡此前 ordinary/SAVE 权重、只保留隐私信号，并删除卡片/收藏/提醒、写隐私屏障及照片抑制。API 34 真实 Debug UI 从四个选项变为“已反馈 · 有意思”，强停重启后仍成立；取消隐私弹窗保持 `card=1/state=LIKE`，确认后为 `card=0/outbox=TOO_PRIVATE/state=0/photo=NEVER_ANALYZE/suppressed=1/affinity=-0.75`。`.tooling/feedback-experience-audit/audit.json` 为 `GO`、只含合成无照片夹具，SHA-256 `3d3c78932772ee175b760ca9815f1f485a3fe38d9c8b6adc005123b20a44fd45`。完整回归 JVM 126/126、API 34 instrumentation 48/48（App 2、Data 46）、源码守卫 GO、App Debug/Release Lint 0 error（23/9 warning）、R8 Release 成功；Debug/未签名 Release SHA-256 为 `4e77442202133dd8e06cda171e4d1c49ff50139145f03d803726f2d21b2ff87d` / `38c04c30805772eb855b4b0faafd8cb3addf4fb1c6f2b5c4f8469a7aedcbac38`。审计包、夹具、模拟器和 ADB 已清理，外部 Beta 阻断不变。
- 2026-07-23 首次体验已产品化为“价值预览 → 端侧隐私/质量流程 → 兴趣与授权入口”，不再是三页纯文字权限表单。自动发现和仅选择照片的范围分别解释，兴趣选择在 320dp/2× 字体下纵向回流；可见返回与系统返回均回到前页。实跑发现并修复跨页继承滚动位置的问题。最终 picker-only 进入系统 Photo Picker，取消后 3 项兴趣和 `completed=true` 持久化，进入“仅手动选择 / 先选择一张照片”首页；`.tooling/onboarding-experience-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `0cbaec57684a272a0611a67964f1fd18e23d089ee88b2996a1a73d9af3471c26`。完整回归 JVM 120/120、API 34 instrumentation 46/46、App Debug/Release Lint 0 error（32/8 warning）、Debug/R8 Release 成功；Debug/未签名 Release SHA-256 为 `43331e8044c5f400c5bed6c6b2b198ef7f121dc914646667cdc24f1459e3911d` / `1634491b9f53276316d37603bcfd63c1b753a5c91286d2023ca3b00e37a3ce97`。测试包、显示设置、模拟器与 ADB 已清理，外部 Beta 阻断不变。
- 2026-07-23 桌面组件已从基础信息块产品化为见微主触点：2×2 扩大照片，4×2 使用全高照片、紧凑对象/把握、正文、来源和底部换卡控制；`compactLabel` 由 domain 统一生成，避免低置信度标题重复对象名。API 34 Pixel Launcher 实际完成系统 Pin、2×2→4×2 拖拽、两次换卡和精准回卡；实跑发现并修复照片未撑满及右栏包裹内容造成换卡按钮悬空。`.tooling/widget-experience-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `35bb0ee19d22c2f45377cdc8a2ea41680a42efbbea65b6f29250732021efabd2`；清理经重启确认包、绑定与夹具不存在，模拟器/ADB 已停止。完整回归 JVM 119/119、API 34 instrumentation 46/46、App Debug/Release Lint 0 error（32/8 warning）、Debug/R8 Release 成功；Debug/未签名 Release SHA-256 为 `a16cbb12160ff98f8cabdd8b46cf0eb1e89d03258bed166c6723e1b4b55a2bf8` / `39f0743c9f70c519399df7bbbf4b318177e3986cf76c96f27c9caecffde94fe4`。外部 Beta 阻断不变。
- 2026-07-23 已建立首个本地 Git 产品基线：根提交 `5f66df607b4cc99c88fbd61d9c74367a9dcf61bb` 固定 589 个文件、78,344 行，归档 SHA-256 为 `be81cda29e9227d9866e53ce640ccb2dda04082a9a63913cd1a9857ecfa8f6d4`；工作树、Git 对象、敏感文件名/常见私钥模式和 macOS PostgreSQL 门禁脚本的 `100755` 模式均已复核。仓库未配置远端、未推送，该提交只提供本地可追溯工程基线，不构成 Release/Beta 证据。
- 2026-07-23 知识卡已从长表单式按钮堆叠重构为阅读优先层级：照片、标题/收藏状态、识别把握、核心知识、推送原因/来源、收藏/提醒、反馈、安装级排除。引导增加品牌副标题和三段进度轨；收藏/提醒在 `<340dp` 或 `fontScale >=1.5` 时纵向回流。API 34 实跑覆盖标准宽度、第三屏引导及精确 320dp/2.0 倍字体，临时 `visual-audit-card` 已删除、显示设置恢复、crash buffer 为空；`.tooling/card-experience-audit/audit.json` 为 `GO`、`releaseEvidence=false`，SHA-256 `3ffeb2a17fcea8ba1fd00e7cad3ec3c0d4ecda3d7c3bd759d5a24fe0c5f45192`。JVM 119/119，Debug/Release Lint 0 error（32/8 warning），Debug/R8 Release 成功；新 APK SHA-256 为 `A7BF5D93AD8C218D101E31C87B78D40F6C0899ABADC0FC8214E5D9DEEB1B5AFB` / `1774C9CDF153F39208F10196778260BADBFD2F20C6B6D31090C27A4B2A882839`。外部 Beta 阻断不变。
- 后端 TypeScript check/build 与 98/98 项基础测试通过；API 契约、供应链、运行预算和源码护栏均为 GO。
- 2026-07-23 在隔离 PostgreSQL 17.10 (Homebrew) 上三次运行全部 13 个迁移，13/13 仓储/升级测试和编译服务 TCP E2E 通过。新增测试会在模拟旧表中执行 `013_card_detected_object_name.sql`，证明旧卡片按 title 回填对象名、空白对象名受约束拒绝，并验证仓储读写保留“扫帚”。macOS 门禁位于 `scripts/run-postgres-integration-macos.sh`，运行后数据库进程已关闭；结果在 `.tooling/postgres-integration-results-macos/`。这仍是本地 PostgreSQL，不是托管云证据。
- Android：31 个 JVM 套件 118/118、API 34 instrumentation 46/46；当前 Debug、R8 Release、真实 Pixel Launcher 组件添加/缩放/两次换卡/精准回卡及 crash buffer 均通过。App/Data Lint 均为 0 error，分别 32/20 warning。
- 真实本地照片闭环已用 Android 系统 Photo Picker 实跑：bundled ML Kit 对真实自行车图给出 `Vehicle/Bicycle/Wheel/Tire/Metal`，敏感标记为空；客户端压缩并清除可见元数据，经同源上传会话进入本地后端，匹配 `bicycle-001`，写 Room 后在 App 与 2×2 Glance 组件展示，组件点击准确回卡，临时对象目录最终为空。没有注入标签或直接写卡。证据在 `.tooling/photo-to-card-e2e/`，且明确 `releaseEvidence=false`。
- 上述实跑暴露 `Canvas: trying to use a recycled bitmap`：旧 `DisposableEffect` 闭包会在状态切换时回收刚发布的新位图。当前改为不可变 `displayBitmap` 生命周期捕获，并新增 `PhotoThumbnailLifecycleInstrumentedTest` 与源码守卫；修复后 App、组件和精准回卡均无崩溃。
- 显式识别对象已贯穿视觉结果、后端卡片、OpenAPI、PostgreSQL 迁移 13、Room 9、Compose 与 Glance；领域阈值固定为 0.72。低置信度标题已经包含对象时，App/组件副标签只显示“识别把握较低”，不再重复“可能是牙刷”；若标题未携带对象则副标签失败关闭地补回对象。精确百分比保留在 App 无障碍语义中。API 34 已验证 App、2×2 组件和组件精准回卡；`.tooling/recognition-presentation-audit/audit.json` 明确标记 `releaseEvidence=false`。
- 首卡链路不再使用易丢失的字符串提示：domain 定义 QUEUED/SCANNING/FILTERING/SYNCING/READY/NO_MATCH/RETRYING/FAILED，data 只持久化聚合计数与用户安全文案，Worker 在成功、无匹配、有界重试耗尽和权限撤销时收束终态，Compose 映射为可执行的选择/恢复/重试动作。API 34 证明 Repository 重建后状态一致且 SharedPreferences 不含照片 ID、标签或文件名；组件在排期缓存过期后继续显示最后一张卡并标记“新卡缓存已用完”。源码门禁输出 `truthfulAnalysisState=1 widgetCacheExhaustion=1`。
- OCR 敏感信息规则已从“去普通空格后匹配”升级为 NFKC 归一化、全空白移除和有限身份证/银行卡分隔符折叠；覆盖全角/分组身份证号、带品牌或裁掉品牌的分组银行卡号、身份证版式标记，并以日期+手机号负例控制误杀。API 34 使用 bundled ML Kit 对生成的最终 JPEG 字节真实 OCR，确认分组卡号被标记为 `bank_card`；源码门禁输出 `ocrSensitiveNormalization=1`。
- MediaStore 的近 90 天边界已失败关闭：存在 `DATE_TAKEN` 时严格按拍摄时间判断；缺失或为 0 时，只有 `DATE_ADDED/DATE_MODIFIED` 至少一项仍在窗口内才纳入。API 34 真实 MediaProvider 四组合回归同时证明旧无拍摄时间图片被排除、近期无拍摄时间图片被纳入、旧拍摄时间不会被近期导入时间覆盖、近期拍摄时间不会因旧元数据被漏掉；源码门禁输出 `mediaStoreRecencyBoundary=1`。
- “仅选择照片”不再以必须选中至少一张为完成条件：用户选定该模式时即持久化引导与兴趣，系统 Photo Picker 回调只负责导入。API 34、320dp/2× 字体实跑确认取消 Picker 后返回“照片权限：仅手动选择 / 先选择一张照片”首页，`completed=true`，五类照片分析 WorkManager 任务计数为 0，crash buffer 为空；Windows accessibility smoke 已固定该路径。
- 2026-07-23 API 34 原生界面审计已将知识卡恢复为首屏主内容：四项反馈与提醒入口无需先穿过组件推广；组件 CTA 只在每日页第一张卡之后出现，收藏页不重复；照片与云端管理默认收敛为“你的数据与隐私”，展开后原六项能力完整可达。空每日、空收藏、每日卡、收藏卡和展开隐私均以真实模拟器截图及 accessibility hierarchy 验证。追加的 320dp/2× 字体审计发现横排 CTA 会把标题挤成逐字换行，现按宽度/字体倍率切换纵排；标准宽度保持紧凑横排。CTA 完整命名为“添加桌面组件”并实测进入 Pixel Launcher Pin Widget；每日/收藏页签会向 accessibility tree 暴露随页面切换的 `selected=true`。
- 320dp/2× 下每日页签改用单行“每日”，无障碍名称仍为完整“每日卡片”且保持 `selected=true`；411dp/1× 继续显示“每日卡片”。两种布局均以截图和 hierarchy 复验。
- 2026-07-23 真实 Pixel Launcher 组件闭环：App 普通每日页全量滚动不显示未来缓存；从组件点击时允许且只允许聚焦组件当前卡。4×2 组件从今日卡开始，只把当前卡和最多两张未来卡放入换卡池，历史卡不再混入且到末尾不循环。DataStore 状态流解决同一 Glance 会话边界漏重绘；实测两次点击均在 3 秒内更新，最终显示“今天已换 2 次”且按钮消失，点击卡片准确打开第二张未来卡。源码门禁输出 `futureCardCacheHidden=1 widgetSwitchAffordance=1 widgetLiveRefresh=1 widgetCardDeepLink=1`。
- 当前 Debug APK SHA-256：`99420560f24c5feafb4d779e8580c1333d1c7663316ab4b71f914627ced17deb`；未签名 Release 为 `6b8f8189b3a4aa0462ebc511252300878ffa8e72435e548d0531bd5bfdc8e897`；App/Data instrumentation APK 为 `4ffa7b1dd437115cb057a3c96e1a29732d216e8c09ecffb8c00a3c06e9c2ea70` / `aa8a793aaaedaea5d7988dd698c8aa2ea8142df59198993c87bc967941ad529b`。`formalSigning=0`，均不可作为 Beta 正式包。
- 本地收藏完成本轮 Loop Engineer：Room 7→8 引入带卡片级联外键的 `saved_cards`；首次收藏只发一个 SAVE 偏好，取消/重启/服务端 upsert/再次收藏保持幂等。第一次 critic 为 `REVISE`，指出 TOO_PRIVATE 会留下 SAVE outbox；修复后第二次最终 critic 仍为 `REVISE`，指出隐私屏障和删除分属事务。当前已合并为首个执行的单一 Room 事务，并新增“事务提交后立即崩溃、重启”API 34 回归；达到两次 critic 上限后未虚构第三次 PASS。
- TOO_PRIVATE 的本地提交点会一次写入隐私 outbox、将照片标记为 `NEVER_ANALYZE` 并抑制后续扫描，再删除提醒、普通反馈、卡片和级联收藏。真实 Debug UI 验证从 `收藏 1 / SAVE=1` 到 `cards=0 / saved=0 / feedback=TOO_PRIVATE / suppressed=1`，进程重启前后均成立。
- 卡片详情来源链已完成 evaluator–optimizer，第一次 critic 为 REVISE、第二次最终为 GO。后端目录与 Android 同步/旧缓存/点击只允许公共 HTTPS，拒绝深链、凭据、本机/内网名称和直接 IP；全部分页先验证后单次写 Room，第二页恶意来源不留下半更新。OpenAPI 的 `Source/Card/ErrorResponse` 已从错误嵌套恢复为顶层 schema；源码门禁为 `safeKnowledgeSourceLinks=1 apiSchemaStructure=1`。
- 小组件每日最多换两次由 `WidgetStateStore` 的 DataStore 事务状态机强制执行；32 次并发点击与 32 次刷新只提交两次，旧偏好迁移、进程重建、卡片移除、跨日和迟到旧日回调均有 JVM/API 34 回归。当前状态机到换卡池末尾返回无更多卡，不会循环回当天已展示卡；组件直接观察持久状态流解决活跃 Glance 会话漏重绘。
- 小组件自动刷新已从“进程启动时刻 + 24 小时”改为按 `Asia/Shanghai` 00:05 为未来 7 个自然日分别建立唯一 OneTimeWork，任一任务执行后补齐后续窗口，离线读取 Room，系统组件更新保留为兜底。最终 critic 为 REVISE，指出立即任务的 `REPLACE` 自取消竞态；现已统一使用 `KEEP`，重复排队保留未完成任务 ID，源码门禁为 `calendarDayWidgetRefresh=1`。两次 critic 上限已用尽，未虚构第三次 PASS。
- 未来七天卡片排期不再使用历史卡总数；任务完成事务以每设备 PostgreSQL advisory lock 选择今天起首个空日期，并严格归一化数据库 `date`。四连接池 32 路并发产生连续唯一日期，删除中间日期后下一张补最早缺口；独立 critic 最终为 PASS，源码门禁为 `contiguousCardSchedule=1`。
- 云端删除崩溃恢复完成本轮 evaluator–optimizer：持久化 `DELETE_PENDING/DELETE_CONFIRMED`，服务端以原子 `created` 证明区分令牌轮换与删除后新建替代设备；客户端先等待取消提醒，再按远端确认、Room 原子清理、身份重置执行。最终独立 critic 为 REVISE，指出先重置身份会在 Room 清理前崩溃时丢失恢复材料；已修复并以 JVM 崩溃点、API 34 丢响应/确认重放、源码守卫与 UI 烟测验证。两次 critic 硬上限已用尽，未虚构额外 PASS。
- 图片上传状态保真完成本轮 evaluator–optimizer，最终独立 critic 为 PASS。原始 OkHttp PUT 会保留非成功 HTTP 状态；401 只刷新身份并重放一次，409/429/5xx 保持 READY、保留 Picker/分享私有副本并进入有界重试，400/410/413/415 才进入 FILTERED 并清理副本。JVM、API 34 和源码守卫均覆盖该边界。
- 详情页/组件缩略图 OOM 风险完成本地 evaluator–optimizer，最终独立 critic 为 PASS。共享 decoder 先读 bounds、应用八种 EXIF 方向并回收中间位图；详情长边 ≤1280 px，组件 ≤320 px/400 KiB。策略测试覆盖 48MP 与极端宽高比，API 34 覆盖损坏输入、方向交换和 20 次重复解码；真实 Pixel Launcher 组件绑定/渲染无崩溃。
- “太私人”同步竞态已修复：隐私 outbox 先提交并在全部卡片分页期间保持为硬屏障，按 card ID 和 `NEVER_ANALYZE` 双重拒绝陈旧卡；只有全分页成功后才确认 outbox，失败/中断保留供幂等重试。独立 critic 最终指出清索引边界后为 REVISE；修复达到本轮复核硬上限，未虚构额外 PASS，25/25 设备回归包含成功和 503 两条真实 HTTP 路径。
- 权限生命周期与上传并发完成上一轮本地 evaluator–optimizer，该轮最终独立 critic 为 PASS。前台恢复会对账真实权限；撤权仅取消自动链并保留 imported；FULL→PARTIAL 使用专用 reconciliation；自动/显式导入上传来源隔离且非法范围失败关闭；单进程上传由可取消互斥串行执行，进程重启后 READY 候选仍按原范围恢复。运行态证明 `preConsentAnalysisWork=0`、`deniedAnalysisWork=0`、`revokedAutoWork=0`、`partialScope=1`、`partialReconciliation=1`。
- Android 上传只允许 API 同源一次性会话，且 OkHttp 普通/HTTPS 重定向均关闭；单测和源码守卫覆盖。
- 生产日志最小化、非测试环境依赖覆盖拒绝、同候选成本幂等、分享确认前 MediaStore URI 日志泄漏修复均已回归。
- 34 个证据/安全自测及 API 契约、运行预算、供应链、部署清单、源码守卫全部 GO。
- 已按 Beta.62 当前目录生成首批 20 条全空决策人工审核包 `.tooling/knowledge-review-batches/beta62-review-01-pending.json`，覆盖砧板、牙线、海绵、牙刷、安全气囊、自行车和自行车刹车，风险构成为 health 10、safety 6、general 4；包内没有预选决定、来源勾选、语义确认或审核人，不能授予发布资格。
- Kimi 已完成 20 个 SAFE_PACKET 轮次并达到硬上限；报告在 `reports/kimi-adversarial-review-round-20.md`，结论仍为 `NO-GO`。

## 当前阻断

- 百炼按量付费 Key 与 Qwen 模型权限已验证可用，但 AI 安全护栏和所需服务关联角色尚未开通；在此之前，生产 Provider 会按设计失败关闭，不能形成真实 Qwen 安全管线放行证据。
- 知识目录 200 个主题、624 条事实；613 draft、11 仅状态 approved、0 真人签注、0 ready topic。未提供受保护的 `JIANWEI_KNOWLEDGE_REVIEWER_IDS`。
- 已有与当前目录绑定的全目录 531/531 来源可访问证据，但来源可访问不等于语义支持；624 条事实仍未经过受保护白名单中的责任人逐条审核。
- 只有一张 CC0 图片的本地工程闭环；仍没有 300–500 张明确授权的隐私/识别评测集，也没有真实 Qwen/OSS 生产管线结果。
- 没有真实托管 PostgreSQL/OSS/Qwen/HTTPS、不可变 OCI/base-image 摘要和三方签名部署回执。
- 没有正式 APK 私钥签名、华为/小米/OPPO 或 vivo 七天实体机矩阵、真人 `zh-CN` TalkBack 听读、200 卡人工抽检和 10–50 人 cohort。
- 当前失败关闭结果：知识发布、容器部署输入、Beta 原始证据三项门禁均退出 1；`evaluation/beta-evidence.json` 不存在。

## 下一步最短动作

1. 先在当前北京百炼业务空间开通 AI 安全护栏并完成服务关联角色/安全管理授权，然后重跑 `backend` 的 `pnpm verify:qwen-provider -- --credentials-file <csv> --image <authorized-jpeg> --confirm-authorized-image`；只有携带生产护栏的首次调用成功，Qwen Provider 烟测才算完成。
2. 若先做内容：由用户在受保护环境配置真实 `JIANWEI_KNOWLEDGE_REVIEWER_IDS`，按 README 的工作台命令从首批 20 条启动真人审核，逐条打开来源、形成并应用决策；每批后运行 `node scripts/check-knowledge-readiness.mjs`。不要把当前合成 Chrome 回归或 URL 可达性当作审核结论。
3. 若先做云：提供真实测试环境的 HTTPS API、临时 OSS STS 与托管 PostgreSQL，按 `docs/DEPLOYMENT.md` 部署，并按 `docs/BETA_EVIDENCE_RUNBOOK.md` 生成已签名部署回执和安全/敏感双样本证据。
4. 若先做设备：用正式签名 APK 在华为、小米、OPPO/vivo 采集七天原始报告，再编译实体机与 TalkBack 工件。
5. 所有真实工件齐备后组装 `evaluation/beta-evidence.json`，只有 `node scripts/check-beta-readiness.mjs evaluation/beta-evidence.json` 返回 GO 才可 Beta。

## 不可重复的坑

- Windows 下必须从 `D:\wyq\jianwei` 运行 Gradle；混用桌面 junction 会造成 Gradle 根目录冲突。
- PowerShell 脚本需用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File ...`，不要修改系统执行策略。
- Android 参考套件不包含全量 JVM 测试；还要单独运行 `:domain:test :data:testDebugUnitTest :app:testDebugUnitTest`。
- 直接运行 Gradle 时还必须设置仓库内 `.tooling\gradle-home`；优先调用 `scripts\build-android-windows.ps1`，不要使用全局 Gradle 缓存后再放宽依赖校验。
- Kimi 已到第 20 轮硬上限，不要重跑或把本地检查记作第 21 轮；不要向外部发送源码、路径、照片或密钥。
- 当前 macOS 隔离工具链在 `.tooling/macos-*`；ADB daemon 需要在允许本地端口绑定的上下文启动。若 R8 测试包与 Debug 签名不同，先只卸载测试 AVD 中的 `cn.jianwei.app`，不要清理真实设备数据。
- 两个 Windows 设备测试脚本的最低计数仍保持 40，本轮没有在 Windows 重跑，因此不要改阈值或误报 Windows 参考套件已覆盖新增测试；当前 macOS API 34 完整实跑总数为 59（App 8、Data 51）。
